# 0.5.0 CUDA for ImageJ

## 0.5.0 之后的小修（2026-09-20）

**命令行 `--headless-run` 的一个参数解析缺陷已修，界面与测量结果都不受影响。**

`src/worm_roi_gui.py` 的 `headless_text()` 只捕 `IndexError` / `TypeError`，**漏了
`ValueError`** —— 而 `sys.argv.index(flag)` 在找不到开关时抛的正是 `ValueError`。
后果：**任何不带 `--brightfield-checkpoint` 的 `--headless-run` 会立刻崩掉**，
报 `ValueError: '--brightfield-checkpoint' is not in list`。

修法是把 `ValueError` 加进 `except` 元组，与相邻的 `headless_float()` /
`headless_int()` 取齐（那两个本来就是三项元组，只有它不同）。**只影响命令行批量跑，
图形界面走不到这段代码。**

因此重打了 EXE 与发布包：`dist/Auto_Worm_ROI.jar`（`ba82bab6…`）与随包权重
**一个字节都没变**。**需要用 `--headless-run` 的用户请换用这一版**；只用图形界面的
不受影响，可以不换。

## 0.5.0（2026-09-19）

这一版加了一个功能：**明场ROI**。多层 TIFF 不再需要先拆页 —— 勾选左栏「明场ROI」后，
开始处理时程序问一次（整批通用）荧光层和明场层分别是第几层，**明场层用来圈 ROI，荧光层
用来测量数值**。随之替换的是多层 TIFF 的处理策略：未开启明场ROI 时，多层 TIFF 会被一句
专门的提示挡下（见下），不再是 0.4.x 那句笼统的「图像格式不支持」。

**明场模型随包发布**（`models/brightfield-0.1.0/` 的 `worm.pt` + `tip.pt`），基于低清
`0.2.2` 用用户的 `明场样例1`（6 张 1024×1024 人工标注）微调而来。这两个文件缺任何一个，
明场ROI 都会明确报错并拒绝启动 —— **绝不回退到高清/低清模型**。一个用荧光权重去分割明场
采集的做法会圈出看起来完全合理、实际不对的 ROI，这是下游任何一步都发现不了的错误。
这不是假设：**未微调的 `0.2.2` 直接跑同一批明场图，六张一张都没数对**（真值 15、16、
16、15、16、11，数出来 24、22、25、25、25、16）。

### 训练：明场模型（`models/brightfield-0.1.0/`）

数据是用户给的 `明场样例1`：6 张 1024×1024 单层明场 TIFF + 6 个人工 `RoiSet_*.zip`
（每张 ROI 数 = 虫数 + 1 个背景区）。**这批是单层的**——只有明场层被导出，文件名里的
`lay2` 记的是它在原多层文件里的层号。因此它能训模型，但**不能用来跑 0.5.0 的两层路径**；
那一步仍需要一张真正的多层 TIFF（见「尚未完成」）。

| | 未微调的 `0.2.2` | 明场 `brightfield-0.1.0` |
|---|---|---|
| 归一化 | `background_aware`（左条带中位数当黑电平，实测把明场压成全黑：均值 16.8/255） | **`legacy`**（明场实测 2% 分位 0、98% 分位 225，虫体仍是一块可分的暗区） |
| 6 张全部数对 | 0/6（数出 24、22、25、25、25、16） | **6/6** |
| 留出 2 张（16 条 / 11 条）数对 | 0/2（数出 25 / 16） | **2/2** |
| 留出平均 Dice / tip Dice / 端点误差 | 0.6901 / 0.5876 / 0.1767 | **0.9275 / 0.8581 / 0.0238** |
| 随包权重在**六张训练图**上自评：平均 Dice / tip Dice / 端点误差 | — | 0.9512 / 0.9027 / 0.0168 |

- 起点：`models/0.2.2/worm.pt`（整虫）、`models/0.2.2/tip.pt`（tip），工作分辨率 768。
- 划分 4 训练 / 2 留出（留出的是 n=16 与 n=11 两张；**n=11 训练集里没有**，所以「虫子少
  的图也数得对」是真泛化，不是背下来的）。留出数字取自这一版；随包的是六张全进的
  train-all 版，两者同配置、不同划分，checkpoint 里用 `validation_provenance` 写清楚了。
  上表最后一行取自 train-all 版、六张全是它见过的图，**是自评不是泛化**；能当泛化读的
  只有「留出 2 张」那一行。
- 试过 `--contact-boost 3.0 --continuity-boost 1.75 --hard-repeat 4` 那一档（这套在 0.4.3
  的低清模型上是用着的），留出集上**每项都略差**（Dice 0.9265、tip Dice 0.8551、端点
  0.0245），未采用。与 0.4.3 `high_v043m` 那次实验同一个结论。
- 明场这一路的**低清分裂器固定关闭**：那个开关的判据是「主 checkpoint 的 sha 在低清指纹
  表里」，与明场权重无关，而分裂器只在低清荧光域上训过。汇总表的
  `low_clarity_split_enabled` 记的是**实际跑没跑**，所以明场批次这一列是 0。

### 训练（第二轮）：加入 `明场样例2` 的 7 张 —— 试了，但**没有采用**

后来又给了 7 张同样标注的明场图（`明场样例2`：`l4440-EGTA-1~5`、`sta2-ctr-2/-3`），
于是合成一个 13 张的数据集重训，看能不能比随包的 6 张版更好。**结论是不能：随包权重仍然
是 `brightfield-0.1.0`，`models/` 一个字节没动，EXE 与发布包也没有重打。**

做法：`build_brightfield_dataset.py` 合成 13 张，留出 4 张（`ctr-3`、`ctr-6`、
`EGTA-3`、`sta2-ctr-3`）。评测参数先用上一轮那个留出模型反推锚定 —— 它在本轮数据集上
复现出记录里的 0.9275 / 0.8581 / 0.0238，据此确认是
`--erosion 3 --min-area 0.003 --max-instances 32`，两侧的评测都用的这一套。

| 模型 | 训练数据 | 4 张留出：虫数 / Dice / tip Dice | 两张新域图（`EGTA-3`、`sta2-3`，五个模型都没见过） |
|---|---|---|---|
| **随包 `brightfield-0.1.0`** | 6 张（全 `l4440-ctr`） | 4/4 · 0.9429 · 0.8864 | 2/2 · **0.9367 / 0.8734** |
| 上一轮的留出版（4 训练 / 2 留出） | 4 张（全 `l4440-ctr`） | 4/4 · 0.9296 · 0.8582 | 2/2 · 0.9317 / 0.8582 |
| 本轮 9 张，cold 150 轮 | 9 张（4 ctr + 4 EGTA + 1 sta2） | 4/4 · 0.9163 · 0.8073 | 2/2 · 0.9243 / 0.8266 |
| 本轮 9 张，warm 150 轮（从 `0.1.0` 续训） | 同上 | 4/4 · 0.9166 · 0.8115 | 2/2 · 0.9258 / 0.8336 |
| 本轮 9 张，cold 300 轮 | 同上 | 4/4 · 0.9208 · 0.8270 | 2/2 · 0.9311 / 0.8508 |

- **虫数一张没错。** 随包模型在这 7 张新样例上数出的是 15/15、15/15、15/15、14/14、
  14/14、15/15、16/16，合起来 13 张全部 `count_correct=True`。「这批新样例能不能处理」
  这个问题，`brightfield-0.1.0` 本身已经答完了。
- **加数据没有换来提升，反而小幅下降。** 在两张**任何一版都没见过**的新域图上，加了
  4 张 EGTA 的 9 张版比只用 6 张 `l4440-ctr` 训出来的随包版低 0.005～0.012 Dice、
  0.02～0.05 tip Dice。三个配方（cold / 从 `0.1.0` 续训 / 300 轮）方向一致，300 轮
  最好但仍未追上。
- **300 轮那一版是明确过拟合**：`train_loss` 降到 0.1815，而 `val_loss` 从 0.41 涨到
  0.5972，`best_selection_score` 停在 0.8507。9 张图上延长训练压不出新东西。
- **排除了「新样例标注风格不同」这个解释**：13 张的每虫像素面积都在 33k～36.7k 之间，
  新旧两批标注一致。
- 一句话：**这 7 张与原来 6 张是同一套采集条件下的近似冗余样本**，模型从中学不到新东西；
  「再喂一批同条件的图就能变好」这个假设在这批数据上不成立。留出对照的全套产物在
  `training/brightfield_v050_r2/`（`training\` 不入库）。

### 新增：明场ROI

| 项 | 行为 |
|---|---|
| 开关 | 左栏新增复选框「明场ROI（多层 TIFF 需开启）」；设置菜单里也有一个，两处绑同一变量 |
| 层号 | 每批开始时弹两次输入框（荧光层、明场层），**整批通用**；两个层号相同会被拒绝。输入框默认值取配置文件里存的上一次的值，但每批仍然照问 |
| 分工 | 分割（含 tip 精修、归一化、工作分辨率）全部走明场权重与明场层；`Measurements` 的每个数值走荧光层 |
| 单层 TIFF | 混在文件夹里的单层图照常处理，不受层号影响；汇总表里它的 `qc_plane` 与 `measured_plane` 都是 1 |
| 尺寸校验 | 两层尺寸不一致时该图失败并写明原因，不会拿尺寸不同的掩膜去算数 |
| 越界 | 层号超过文件层数：整批拒绝（批量侧）／单图报错（`process_image` 直接调用时） |

### 替换：多层 TIFF 的处理策略

- **未开启明场ROI** —— 弹框「需要打开明场ROI才可以处理多层TIFF」，逐条列出文件名与层数，
  并说明两条出路（开启明场ROI，或先用 `Image > Stacks > Stack to Images` 拆成单页）。
  **输出目录不会被创建。** 这条替代了 0.4.x 里把多层 TIFF 归入「图像格式不支持」的做法。
- **保留的例外** —— 第 1 层之外全是 0 像素的（MetaMorph 空 overlay 平面）照旧放行，只在日志
  里记一句。层号对话框只对第 1 层之外**有数据**的文件出现。

### 监视器与 QC（需求 2）

- 明场ROI 启动时，QC 图是**明场层 + 圈出的 ROI**，不展示荧光 QC。文件名仍是
  `<图名>_QC.png`，与关闭该功能时同一个名字 —— 下游按名字找 QC 的地方一处都不用改。
- 每张 QC 的标题追加 `· 第 N 层（明场）`，面板底部状态行变成 `共 N 张 QC 图（明场）`。
  两者都只在**两个层号真的不同**时才显示：单层图与关闭功能时两个层号都是 1，标「明场」
  会是假话。
- 手动标注视图（决策 5）在明场ROI 开启时改为显示**明场层**，标题同样带层号。
  **已知限制**：标注按**文件名**存（sidecar `<图名>.autoworm.json` 与文件夹 store），
  **不区分层**；同一文件换一个明场层重跑，箭头会沿用。

### 汇总表

`other/batch_summary.csv` 新增三列，位置在 run 元数据列之前：

| 列 | 含义 |
|---|---|
| `brightfield_roi_enabled` | 这一批是不是明场模式（混批里的单层图也是 1） |
| `qc_plane` | 这张图的 QC 取自第几层（永远有值） |
| `measured_plane` | 这张图的数值取自第几层（永远有值） |

run 元数据列新增 `brightfield_model_sha256`，紧跟 `worm_model_sha256`；未使用明场权重时
为空串。

### 跨语言契约：桥接协议升到 2

- 通知文件新增 `qc_plane=` 与 `measured_plane=` 两行（裸十进制）。**缺键 = 第 1 层**，
  与 0.4.3 及更早的界面行为一致。
- Java 插件新增 7 参 `measureOutputFolder` 重载（末尾 `int measuredPlane`），6 参版本转调它
  并传 1；测试里的既有调用点零改动。每张图在 `IJ.openImage` 之后按层号 `setSlice`，
  **越界一律跳过**而不是照测 —— 测错层会得到一组看不出错的数字。
- **新界面 + 旧 jar 是最危险的组合**：旧 jar 不认识层号，会一律测第 1 层。所以明场ROI 开启
  且桥接协议不是 `"2"` 时，**点开始处理会被拒绝**（「需要同版本的 ImageJ 插件」）。
  关闭明场ROI 时新旧混搭无害，不拦。

### 接口变更

- `process_image` 返回值 **29 项 → 32 项**（末尾追加上面那三列）。
- `run_gui_batch` 新增 `brightfield_roi` / `fluorescence_plane` / `brightfield_plane` /
  `brightfield_checkpoint_path` / `brightfield_tip_checkpoint_path`，全部有安全默认值。
- CLI 新增 `--brightfield-roi` / `--fluorescence-plane N` / `--brightfield-plane M` /
  `--brightfield-checkpoint PATH` / `--brightfield-tip-checkpoint PATH`。
- `enhanced_tiff_rgb(path, plane=1)` 新增可选层号参数，默认值保证既有调用点行为不变。
- 明场ROI 开启时的两行日志改了措辞：低清分裂器那行说成
  `enabled for single-plane files only (never on the brightfield plane)`（混批里的单层图
  仍然用它，多层图这条路径固定不用）；明场模型设置那行从「differ」改成
  「每个 checkpoint 自带设置」——明场按设计就是 `legacy`、低清荧光是 `background_aware`，
  这行几乎每批都会出现，原来那句话读起来像出错。
- 新增两个开发工具（不在发行包里）：`src/build_brightfield_dataset.py`（建只含明场的
  数据集，不并荧光基座）与 `src/finalize_brightfield_model.py`（一次写 worm + tip 两个
  文件，并硬校验 `normalization_mode == "legacy"`）。

### 测试

- Python：159 → **168** 条（新增 `BrightfieldRoiTests` 9 条）。
- Java：16 → **19** 条（新增 `theRequestedPlaneIsTheOneMeasured`、
  `aPlaneBeyondTheStackIsSkippedAndReported`、`aSinglePlaneImageIgnoresThePlaneNumber`）。
  第一条双向验证：同一张两页文件（第 1 页 100、第 2 页 10）分别按 `measuredPlane=2` 与
  `measuredPlane=1` 测量，`Mean` 分别是 10 与 100。

### 构建与发布包

本轮改了 `src/*.py` 与 `plugin_src/*.java` 两边，所以 EXE 与 jar 都重打了（互不替代）。

- **jar**：`javac --release 8 -encoding UTF-8` 编译 `plugin_src/*.java` 后用 `jar` 打包，
  25,163 字节。
- **EXE**：PyInstaller 打包 → `dist\AutoWormImageJ\`（3.2 GiB），`AutoWormGUI.exe`
  47,977,809 字节；随后 `postbuild.py` 同步 CRT DLL、`copied models ->`、`copied licenses ->`。
  （这个字节数是 2026-09-19 那次构建的；**2026-09-20 修掉上面那个命令行缺陷后重建为
  47,977,795 字节**，见本文档开头「0.5.0 之后的小修」。）
- **新构建的回归**：Python 全量 **168/168 通过**（跑的就是新构建的树）。
- **`make_release.py`**（`--previous-exe` 指向 `0918 0.4.3` 的 EXE）全部闸门通过：

  | 闸门 | 结果 |
  |---|---|
  | 版本号 / 文档名 | `version: 0.5.0-beta (menu), docs named for 0.5.0` |
  | jar 是否等于当前 Java 源 | **9 个 class 全部 match**（常量池、方法、字节码逐项比对） |
  | EXE 是否等于当前 Python 源 | **13 个模块全部 match，0 differ** |
  | 比对是否有效（对照组） | 0.4.3 的 EXE 有 **3 个模块 differ**（`batch_worm_roi`、`manual_head_annotation`、`worm_roi_gui`），所以上一条不是空比对 |
  | 打包 | 14,767 个文件，3.14 GiB → **2.06 GiB** zip |

- **发布包**：`release\AutoWorm-0.5.0-beta-ImageJ.zip`（14,767 个文件，解压 3.14 GiB）。
  - 包内 `plugins\Auto_Worm_ROI.jar`（25,163 字节）
    `ba82bab687507dec719bf9f2ee1d538e1a4e66608a3235c829ddf929d70423b4`
  - 包内 `AutoWormGUI.exe`（47,977,795 字节）
    `d15aee5c01140d8a5a8bc9967f774b4944158b742e185cbc89168d5f65c9beb2`
    （初版是 47,977,809 字节 `f4519b90eda26fc0…`；2026-09-20 修掉本文档开头那个命令行
    缺陷后重建，上表已按重建后的值更新。jar 那一版 `ba82bab6…` 没变过。）
  - **包内 `plugins\AutoWormImageJ\models\brightfield-0.1.0\` 的 `worm.pt` 与 `tip.pt`
    与源目录逐字节相同**（sha256 分别是 `3c295da3…` 与 `f2845967…`），且包内 README 是
    改过明场措辞的那一份 —— 「打出来的包确实带着明场权重」这件事有证据了。
  > jar 与 EXE 的 SHA256 只对得上「这一个文件」：`jar` 会把每个 class 的打包时刻写进条目头，
  > PyInstaller 产物同理。换台机器重打，版本号一致而哈希不同是正常的；要核对版本请解压
  > `plugins.config`，或直接跑 `make_release.py`。
  > **zip 自己的 SHA256 不写在这里**：这份文档就在包里，写完再打一次包，包和它里面记的
  > 哈希就又不相等了。zip 的体积与 SHA256 记在不随包发布的
  > `devdocs/AGENT-0919-0.5.0.md`。
- **执行方式的一点如实说明**：`build_0.5.0.bat` 这一步是在 git-bash 里**逐条执行它的四个
  步骤**（javac → jar → PyInstaller → postbuild，同顺序同参数）完成的，不是字面意义上的
  `build_0.5.0.bat` 调用 —— `cmd //c` 在这台机器上会吃掉 `/c` 开关或把反斜杠弄坏。结果与
  脚本一致，但「bat 本身能被双击运行」这件事本轮没有验证。

### 尚未完成

- **两张验收仍未做**：①用一张**真正的多层 TIFF**（荧光层 + 明场层各占一层）走完整套 GUI
  流程，人眼确认监视器 caption 的层号、`batch_summary.csv` 的三列、以及 ImageJ 量出来的
  数值与手动 `setSlice` 一致；②150% 缩放下左栏多出来的「明场ROI」复选框不挤。
  自动测试与命令行端到端都过了，缺的是 GUI 上的人眼那一步。
- **已装入 `E:\fiji-win64`，但还没启动过**：装之前那里是 0.4.3（jar 24,413 字节
  `8bd27a88…`），现在是 0.5.0（jar 25,163 字节 `ba82bab687507dec…`），
  `AutoWormGUI.exe` 换成 47,977,809 字节 `f4519b90…`，两个明场权重也随包进去了。
  （**这段记的是 2026-09-19 装入时的状态；随后那份 EXE 在 2026-09-20 重建过，
  已装的副本没有跟着更新**，要不要更新由用户决定。）
  做法是**整棵删掉 `plugins\AutoWormImageJ\` 再解压**（不是就地覆盖），
  另把根目录的 0.4.3 三份文档删掉、换成 0.5.0 的。**需重启 Fiji 才生效。**
- **未推送 git**（用户要求）。

## 0.4.3（2026-09-18）

这一版只有一件事：**两个整虫模型和它们各自的 tip 精修器，全部用新的 `补充样例5` 微调过**，
随包权重随之替换。此外只有一处行为改动 —— 候选实例上限 12 → 32（见下）。界面代码、输出
目录结构、测量表列序、Java 插件**与 0.4.2 逐字节相同**。

训练目标是**虫数不等于 10 的图**。0.4.2 的两个整虫模型都是按「每张十条虫」的老域调的，
用户报上来的新样例里 n 从 15 到 19 都有，模型不是数少就是数多。这一版拿 `补充样例5` 的
`train/` 微调、拿 `test/` 当验收标准。

### 训练数据

`补充样例5`（开发机上的内部样例，不在发行包里）分 `train/` 与 `test/` 两个文件夹，**全是
2048×2048 的采集**。`train/` 4 张 TIFF + 4 个 `RoiSet_*.zip` + 4 份 `Results_*.csv`；
`test/` 2 张 + 2 个 + 2 份 `answer.txt` 里写着真值。全部是人工标注（ROI 名是 ImageJ 自动
编号，不是插件写的 `01_worm.roi`），可以直接当 ground truth 用。

| 数据集 | 记录数 | 划分 | 来源前缀分布 |
|---|---|---|---|
| `training/high_v043` | 58 | 训练 54 / 留出 4 | `base` 12、`supp` 34、`supp4` 8、`supp5` 4 |
| `training/low_v043` | 108 | 训练 94 / 留出 14 | `base` 12、`boundary3` 20、`new` 30、`supp` 34、`supp4` 8、`supp5` 4 |

两个模型都是**从上一版微调**（高清 `0.1.1` → `0.1.2`，低清 `0.2.1` → `0.2.2`），
`normalization_mode` 与工作分辨率 768 都不变。

> **`补充样例5` 的合理归属是低清域**：本项目高清域的三个样例（样例1、补充样例1、
> 补充样例4）**全是 1024×1024**，低清域的两个（补充样例2、补充样例3）全是 2048×2048。
> 这份样例是 2048²，也就是低清域的尺寸。用户要求两个模型都训，因此两个都训了 —— 但下面
> 高清那条短板的根因就在这里。

### `test/` 上的验收结果

`test/` 两张图，真值来自同目录的 `answer.txt`：

| 图 | 真值 | 低清 `0.2.2` | 高清 `0.1.2` |
|---|---|---|---|
| `ADF-DR+odor` | 16 | 16 ✓ | 16 ✓ |
| `AWC-DR` | 19 | 19 ✓ | 18 ✗ |
| **全对** | | **2/2** | **1/2** |

**低清模型达标。** 它在 `补充样例5` 全部六张上也都数对了（16 / 19 / 17 / 17 / 15 / 16），
而它的起点 `0.2.1` 只对四张。

**高清模型差一张，如实记下。** `AWC-DR` 高清数出 18、真值 19。原因不是训练不够，是**域不
匹配**：这张图是 2048²，而高清域全是 1024²。同一份高清权重在自己的 1024² 老域上表现反而
比 0.4.2 好（留出集 4/4 全对，四项指标全部优于 0.4.2，见 `VALIDATION_0.5.0.md`）。把
2048² 的图缩到 768 喂给只在 1024² 上训过的模型，挨着的那条细缝会丢。

这张图是两张 test 图里虫体重叠最厉害的一张，所以还试过另一条路子：把这批图按「重叠合并」
加权重训一版（contact/continuity boost 拉到低清档的量级）。它自己的验证分**更高**
（0.8902 对 0.8884），但在同一套 test 上**还是 16 / 18，一张没多对**。重叠惩罚不足这个
假设因此被排除，剩下的解释就是尺寸。那一版**没有进包**，`models/0.1.2` 仍是原方案。

**没有为了这一条去改高清模型**：高清的 `0.1.2` 在它自己的域上是净改进，为一张 2048² 的图
回退会把它在老域上一起拖坏。用户认可按现状收尾。

### 候选实例上限：12 → 32

`postprocess_max_instances` 两个模型都从 **12 抬到 32**。这是 0.4.2 数不出 n>12 的**直接
原因**：这个值在 `semantic_to_instances()` 里按面积×高度截断候选，n=19 时第 13 条以后根本
进不了后处理，模型再准也数不出来。12 只够数到 n=12。

抬到 32 对 n≤12 的老域不会有「截断少算」的问题（老域本来就用不满 12 个候选），但会让模型
在**本不该多出候选**的图上多出候选 —— 这就是下面那条净代价的由来。

### 端到端回归：老域是净代价，不是净收益

拿 0.4.3 的两个权重在 **11 个老域的 100 张真实图**上各跑一遍完整流水线（形状细化、人工头向
标注、Python 测量后端全开），与 0.4.2 逐图对比 `batch_summary.csv` 的 `worm_count`：
**11 张的输出变了，其中 5 张变好、6 张变差。**

变好的 5 张：

| 批次 | 图 | 0.4.2 | 0.4.3 | 真值 |
|---|---|---|---|---|
| 补充样例2 · 0114 | `Phsp16.2 0101-0-1 n=20` | 12 | 19 | 20 |
| 补充样例2 · 0114 | `Phsp16.2 0101-50-1 n=20` | 12 | **20 ✓** | 20 |
| 补充样例3 · boundary3 | `! hsp4 020901 wdr5-2` | 9 | **10 ✓** | 10 |
| 补充样例3 · boundary3 | `! hsp4 250901 wdr5-1` | 11 | **10 ✓** | 10 |
| 补充样例3 · boundary3 | `hsp4 250619 wdr5-2` | 11 | **10 ✓** | 10 |

前两张正是被 `max_instances=12` 卡住的（真值 20）：抬到 32 之后第一张数到 19、第二张 20 全对。
后三张是低清域上 n=10 的修正。

变差的 6 张：

| 批次 | 图 | 0.4.2 | 0.4.3 |
|---|---|---|---|
| 补充样例1 · Pgst4（**高清自己的老域**） | `Pgst4 0710-10-2 GFP` | **10 ✓** | 9 |
| 补充样例1 · Pgst4 | `Pgst4 Desgly D3 0529-1-2` | **10 ✓** | 9 |
| 补充样例2 · 0114 | `Phsp16.2 0101-90-2` | **10 ✓** | 11 |
| 补充样例3 · boundary3 | `hsp4 250605 wdr5-2` | **10 ✓** | 11 |
| 补充样例3 · boundary3 | `hsp4 250607 N2-1` | **10 ✓** | 11 |
| 补充样例3 · boundary3 | `hsp6 250607 N2-2` | **10 ✓** | 11 |

**这 6 张是这一版的代价。** 其中 4 张在低清域（`补充样例5` 本就是低清域，微调时多出的候选
带到了同域的老图上）；另外 2 张在**高清自己的老域 `补充样例1`** —— 高清的 `0.1.2` 在那批
34 张里丢 2 张、一张没多。逐批明细：样例1 · 0716（高清，8 张）0 张变；补充样例1 · Pgst4
（高清，34 张）2 张变差；补充样例4 · 0913（高清，8 张）0 张变；补充样例2 · 0114（低清，
30 张）3 张变（1 好 2 坏）；补充样例3 · boundary3（低清，20 张）6 张变（3 好 3 坏）。

### 两个 tip 精修器跟着重训

整虫模型换了，精修器的训练输入（guide）必须跟着重出，否则它学的是**旧模型犯的错**。两个
精修器都先用新整虫模型重出 guide，再从头训一遍，超参一个没动。best val patch Dice 高清
0.9380 → **0.9523**、低清 0.9347 → **0.9509**。

### 没有改的东西

界面、`src/worm_roi_gui.py` 的推理与后处理代码、输出目录结构、测量表列序、Java 插件、
`check_image()` 的判定 —— 全部与 0.4.2 相同。这一版只换了 `models/` 下的**两个目录名与四个
权重文件**（外加 `postprocess_max_instances` 这个随权重走的参数）。

`src/worm_roi_gui.py` 与 `src/batch_worm_roi.py` 的改动只有三处：版本号字符串、
`MODEL_CONFIGS` 指向新目录、`LOW_CLARITY_MODEL_SHA256S` 里加一个新指纹（**旧的保留** ——
0.4.2 与 0.4.1 散出去的 `0.2.1` / `0.2.0` 还在用，三个都得认出来）。

> 原来的 `models/0.1.1` 与 `models/0.2.1` **仍在包里**：`postbuild.py` 整棵 `models/` 复制，
> 没有做「只留当前一对」的裁剪（0.4.2 是手工只放了当前一对）。多出来约 24 MB，不影响
> 运行时行为 —— `MODEL_CONFIGS` 指向的是新目录。留着是有意的：删掉的话，任何一次重建都会
> 把它们再带回来，包的内容就不再等于构建脚本的输出。

## 0.4.2（2026-09-16）

这一版有两件事，彼此无关。

一是**模型更新**：高清与低清两个整虫模型、以及它们各自的 tip 精修器，全部用新的
人工标注 `补充样例4` 重新训练过，随包权重随之替换。推理与界面逻辑与 0.4.1 相同，
只有两条为配合新权重而必须动的地方（模型目录名、命令行判定低清的那条判据）。

二是**输出目录结构改了**：输出目录下分成 `measurements/` 与 `other/` 两个子文件夹，
测量表内部的列序也重排成「要用的七个数值在最前」。这件事只动文件放在哪里、列排在第几，
不动任何数值；但**升级后旧目录里的结果不会再被自动找到**，见
[输出目录分成两个子文件夹](#输出目录分成两个子文件夹测量表列序重排)。

此外，第一份发布 zip 打出之后另做了一轮独立综合审查，提出六条 Bug 并全部修掉，**发布
文件重建过一次**（见[外部审查六条修复](#外部审查六条修复不影响测量数值)）。六条同样不
改变测量数值。

### 为什么重训

用户报「0913 那批图的虫数不对」。0.4.1 已经修掉了这批图里 `0913-100-2 green.tif` 的
崩溃（`predict_raw` 里 PIL 只读数组被写入），但把那批图连同人工标注一起量，**模型本身的
分割仍然是错的**：新标注的 4 张留出图，随包高清模型一张都没数对。

| 留出图（补充样例4 的 4 张） | 旧 `0.1.0` | 新（留出权重） |
|---|---|---|
| `0913-0-2` | 数出 7 | 10 ✓ |
| `0913-10-2` | 数出 8 | 10 ✓ |
| `0913-100-2` | 数出 9 | 10 ✓ |
| `0913-1k-2` | 数出 8 | 10 ✓ |
| **虫数全对** | **0/4** | **4/4** |
| 平均 Dice | 0.6719 | 0.9261 |
| 平均 tip Dice | 0.7623 | 0.8662 |
| 平均归一化端点误差 | 0.0315 | 0.0194 |

### 训练数据

`补充样例4`（开发机上的一批内部样例，不在发行包里）= 8 张 TIFF + 8 个 `RoiSet_*.zip`
+ 8 份 `Results_*.csv`，全部来自 0913 这批采集。这里的 ROI 是**人工标注**，不是插件的
输出：ROI 名是 ImageJ 自动编号（`0479-0320.roi`），而插件写出的是 `01_worm.roi`；
`0913-0-2` 的 CSV 列了 10 条虫，而同一张图插件自己跑出来是 8 条。因此可以直接当 ground
truth 用。

新的 `src/build_supplement_dataset.py` 把这份补充样例转成训练用的图像与掩膜。它与既有的
`prepare_boundary_finetune_dataset.py` 有两处不同，都是这份样例的命名逼出来的：

- 样例里 TIFF 叫 `0913-0-1 green.tif`、RoiSet 叫 `RoiSet_0913-0-1.zip`，而
  `prepare_segmentation_dataset.py` 是按**完全相等**的 `<stem>.tif` 找图的，配不上。
  新增 `matching_image()` 容忍 `<stem> <后缀>.tif`，但**要求唯一匹配**，找到 0 个或 2 个
  都直接报错，不会随便挑一张。
- 样例的来源与毛病（`!` 合并、`=` 断裂）记在**基座自己的** `dataset_manifest.csv` 里，
  而按文件名重新推是推不出来的（`classify_issue()` 找的是 `!`/`=`，对
  `boundary3__merge__…` 这样的名字一律返回 `normal`）。因此 `copy_base()` 从基座的
  清单里**照抄** `Source` 与 `Issue`，而不是重算。

两个新数据集的构成与归一化方式（各自与它要微调的那个 checkpoint 保持一致）：

| 数据集 | 构成 | 划分 | `normalization_mode` |
|---|---|---|---|
| `training/high_v041` | 46（原 `_combined_training_n10`）+ 8（补充样例4） | 50 训练 / 4 留出 | `legacy` |
| `training/low_v041` | 76（`_combined_training_v020`）+ 20（补充样例3）+ 8（补充样例4） | 90 训练 / 14 留出 | `background_aware` |

训练都是**从现有 checkpoint 微调**（`--initial-checkpoint`），不是从零重训。超参与各自
的上一版逐项相同（高清 `--tip-boost 4.0`；低清 `--hard-repeat 4 --contact-boost 3.0
--continuity-boost 1.75`），两个 tip 精修器用 `--train-all` 全量重训，训练输入是**新整虫
模型自己吐出来的** guide（换了整虫模型就必须重跑 guide，否则精修器学的是旧模型犯的错）。

> 低清第一次启动时漏了 `--image-size 768`，checkpoint 记成了 512（`boundary_radius`
> 随之变成 4）。**这一版没有发出去**，发现后删掉重跑。之所以要盯这个数：推理侧
> `batch_worm_roi.py` 是从 checkpoint 读 `image_size` 的，512 会静默改变所有低清用户的
> 工作分辨率。`finalize_low_model_042.py` 现在把 `image_size != 768` 直接当致命错误，
> 免得再犯。

### 留出集上的对照

低清 14 张留出图（`boundary3` 的 10 张 + 补充样例4 的 4 张）：

| | 旧 `0.2.0` | 新 `0.2.1` |
|---|---|---|
| 虫数全对 | 9/14 | **10/14** |
| 平均 Dice | 0.8705 | **0.8877** |
| 平均 tip Dice | 0.7857 | **0.8110** |
| 平均归一化端点误差 | 0.0395 | **0.0335** |

**这 14 张里有 4 张的虫数变了，两好两坏，净 +1**，逐张列出来：

| 图 | 旧 | 新 | |
|---|---|---|---|
| `boundary3__merge__g4__14__…` | 10 ✓ | 9 | **变差** |
| `boundary3__normal__g1__04__…` | 11 | 10 ✓ | 变好 |
| `boundary3__normal__g3__12__…` | 10 ✓ | 11 | **变差** |
| `supp4__0913-10-2` | 9 | 10 ✓ | 变好 |
| `supp4__0913-100-2` | 9 | 10 ✓ | 变好 |

变差的两张都来自 `boundary3`（旧低清模型的特长域），变好的三张里两张来自新加入的
补充样例4。把新域的 8 张加进训练集，代价就是在旧域上让出一部分——**没有免费的提升**，
这一点如实写在这里，用之前请按自己的数据判断值不值。

### 端到端回归（三批真实图，走完整条流水线）

比 Dice 更能说明问题的是**端到端虫数**：用户报的就是虫数。三批图分别用新旧模型各跑一遍
`run_gui_batch`，逐张对照 `batch_summary.csv` 里的 `worm_count`：

| 批次 | 张数 | 用的模型 | 虫数有变化的图 |
|---|---|---|---|
| 0913（用户报 bug 的那批） | 8 | 高清 | 0/8 |
| 0913 | 8 | 低清 | 2/8 —— 全部**变好** |
| 样例1（0716） | 8 | 高清 | 4/8 —— 全部**变好** |
| 补充样例2 | 30 | 低清 | 0/30 |

**46 张图共 6 张虫数变化，六张全是改对，没有一张变差。** 逐张：

- 高清 · 样例1：`0716-0-2`、`0716-10-1`、`0716-10-2`、`0716-1k-2` 由 9 条
  `REVIEW_COUNT_MISMATCH` 变为 10 条 `PASS`。这四张是高清模型**原本的训练域**，
  说明这次微调没有只顾新域而丢掉老本行。
- 低清 · 0913：`0913-10-2`、`0913-100-2` 由 9 条变为 10 条 `PASS`。
- 0913 八张在两个模型下都**没有**变差的图；其中 `0913-1k-2` 两版都数出 11 条
  `REVIEW_COUNT_MISMATCH`，这是 0.4.1 就有的既有现象，本次没有触及。

### 随包权重的目录名跟着换了

`models/0.1.0` → **`models/0.1.1`**，`models/0.2.0` → **`models/0.2.1`**，各自含
`worm.pt` + `tip.pt`。旧的两份**不再随包**（0.4.1 的发行包里仍然有）。这样目录名与权重
本身对得上，不会出现「`0.1.0` 里装的其实是第二代权重」这种要翻文档才知道的事。
界面 `MODEL_CONFIGS` 指到新目录，`postbuild.py` 照旧整棵 `models/` 复制 —— 因此随包体积
与 0.4.1 持平，没有变大。

新权重另外补了出处字段（旧的高清权重里一个都没有）：`software_version`、
`training_dataset`，以及

- 高清：`holdout_count_accuracy` / `holdout_mean_matched_dice` /
  `holdout_mean_tip_dice` / `holdout_mean_endpoint_error`
- 低清：`supplement_validation_count_accuracy` /
  `supplement_validation_mean_matched_dice`

**高清那几个数为什么叫 `holdout_` 而不叫 `val_`**：随包高清权重是 `--train-all` 出来的
（`trained_on_all_records: true`，54 张全进了训练），它自带的 `val_*_at_best` 是**在训练
集上**算的，不能当泛化能力读。真正没见过的只有留出那一版权重见过的那 4 张，所以
`finalize_high_model_042.py` 强制要求 `--evaluation` 指向留出权重的评测目录，并给指标换
了前缀。低清不需要这样：它本身就是按 90/14 划出来的，验证集确实是留出的。

权重里 `initialized_from` 原本记的是开发机上的绝对路径，已改写成仓库内的相对
路径 `models/0.1.0/tip.pt`。功能上无影响，但发行包里不该有开发机的目录结构。

### 命令行判定低清的那条判据，从单个指纹改成一组

`batch_worm_roi.py` 里 `LOW_CLARITY_MODEL_SHA256`（单个值）改成
`LOW_CLARITY_MODEL_SHA256S`（元组），同时收录 **0.2.1 与 0.2.0** 两版权重。

这不是顺手扩的：界面每次都把 `low_clarity_split` 显式传进来，但**命令行**不传，靠这个
指纹自动判断。只留新指纹的话，0.4.1 那批已经散出去的低清权重到了 0.4.2 上会被静默当成
高清走另一条分支 —— 正是这条判据当初要防的事。现在两版都认得出，高清权重仍然不命中
（已用三个 SHA 实测）。

### 输出目录分成两个子文件夹，测量表列序重排

输出目录（默认 `_auto_roi`）下面现在固定分两层：

```text
_auto_roi/
  measurements/   <图名>_measurements.csv、ImageJ_measurements_all.csv
  other/          <图名>_RoiSet.zip、<图名>_QC.png、batch_summary.csv、
                  <图名>_shape_qc.csv / _split_qc.csv / _head_annotations.csv /
                  _manual_split_qc.csv / _segment_qc.csv
```

分的是「要拿去统计的」和「看一眼就行的」：测量表常被整批拷走做下游分析，此前它和预览图、
QC 明细混在同一个目录里，拷贝时得按后缀一张张挑。`batch_summary.csv` 是**这次运行的工作
记录**（识别数量、`qc_status`、耗时、`run_id`、失败原因），不是 ROI 的数值，因此与 QC 图
一起放进 `other`，不算测量表。

测量表的**列序**同时按「先给要用的数」重排：`Area`、`Mean`、`Min`、`Max`、`IntDen`、
`Median`、`RawIntDen` 移到第 1 至 7 列，`Index`、`Image`、`Label`、`PixelArea`、
`BackgroundCorrectedMean`、`CTCF`、`RawCTCF`、`Type`、`MeasurementBackend` 依次排在其后。
记录校准的五列（`PixelWidth`、`PixelHeight`、`SpatialUnit`、`ValueUnit`、
`IntensityFunction`）仍然压在行尾 —— `tests/AutoWormMeasurementTest.java` 是按「倒数第
五列起」读它们的，位置一动那个用例就会失败，这是故意的，免得顺序被悄悄改掉。
**数值一个没动**：重排只搬运列，不动算式。本轮拿 `0513-0-1.tif` 走完整条流水线，
Python 自测与 ImageJ 插件两条路量出的同一条虫数值一致（`Area` 都是 `108168`；`Mean`
Python 侧写全精度 `257.9652947267214`，ImageJ 那张表按 `ResultsTable` 的惯例写成
`257.965`）。

Python 自测与 ImageJ 插件两种后端写出的列序一致，但**表头并不完全一样**：Python 侧没有
校准概念，`IntDen` 与 `RawIntDen` 是同一个数，也没有 `Index` / `Image` / `PixelArea` /
`RawCTCF` 与末尾五列校准列。两边共有的部分（前七个数值列）位置相同。

**不兼容旧布局，也不做兼容。** 插件只从 `other/` 里找 `<图名>_RoiSet.zip`，找不到就报
「ImageJ 无法读取 ROI ZIP」并跳过该图（报错信息里会写出它找过的那个完整路径）；测量表
一律写进 `measurements/`，该文件夹不存在时由 `saveTable` 在写之前建好 —— 这样把 ImageJ
指向一个旧版本的扁平目录时是**只读**的，不会在别人的文件夹里凭空造出两个子目录。旧目录
要接着测，就把文件按上表搬进两个子文件夹，或者重跑一遍。GUI 读取 QC 图用的是递归查找
（`rglob`），因此监视器面板在新旧布局下都能找到缩略图；`batch_summary.csv` 的读取路径是
写死的 `other/`，找不到就只是不显示汇总，不报错。

**两个名字有两份。** `MEASUREMENTS_DIRNAME` / `OTHER_DIRNAME`（`batch_worm_roi.py`）与
`MEASUREMENTS_DIR` / `OTHER_DIR`（`Auto_Worm_ROI.java`）是同两个目录名的两份声明，
改一边必须改另一边，没有东西会替你检查。之所以不合并：桥接协议传给 Java 的仍然是**输出
根目录**，两个子目录名由 Java 侧自己拼，Python 不需要知道插件打算写什么。

### 构建脚本：新克隆的仓库里 `dist\` 不存在

`build_0.4.2.bat` 第 1 步直接往 `dist\Auto_Worm_ROI.jar` 写 jar。`jar` 是先写临时文件
再改名过去的，目标目录不存在时抛的是 `NoSuchFileException` 加一串 Java 堆栈，看不出
是「目录没建」。`dist/` 被 `.gitignore` 排除，新克隆的树里没有，于是第一次构建必挂。
现在脚本先 `mkdir dist`，与它建 `build\plugin_classes` 的写法一致。本机这次重建就是
撞上它才发现的。

### 新增的脚本

- `src/build_supplement_dataset.py`：把补充样例（TIFF + RoiSet + Results CSV）转成训练集。
- `src/finalize_low_model_042.py`、`src/finalize_high_model_042.py`：给训练产物盖上产品
  参数与出处，写出随包权重。与 0.3.3 那份 `finalize_low_model_033.py` 的差别是：
  验证指标从**评测目录里现读**（`--evaluation`），不再硬编码在源码里 —— checkpoint 里记
  的数字必然出自那次评测，而不是谁抄进去的。两处会在写盘前校验并回读确认再替换。

### 外部审查六条修复（不影响测量数值）

这一版打出第一份 zip 之后，另做了一轮独立综合审查，提出六条并全部修掉。**六条都不改变
`Area`、`Mean`、`CTCF` 这一串数值**，所以上面各节的对照表与结论继续有效；但**发布文件
重建过一次**（jar 23534 → 24422 字节、EXE 47960362 → 47962761 字节），现行哈希见
`VALIDATION_0.4.2.md` 的「发布文件」一节。

| 编号 | 症状 | 修法 |
| --- | --- | --- |
| BUG-042-01 | 预检与跑批之间目录变了，产物上完全看不出来（ROI 有、汇总表正常、荧光值是空的） | 预检清单作为快照传给跑批 |
| BUG-042-02 | 表头含 `CTCF` 子串的**别人的** CSV 会被当成本插件的旧表删掉 | 按整字段比对，认不出的表不动 |
| BUG-042-03 | 整批全失败时只看到一句「没有可测量的 ROI ZIP」，真实原因被吞掉 | 先显示真实原因再收场 |
| BUG-042-04 | 有障碍横穿条带时整张图被判「找不到干净背景区域」，尽管旁边还有大片干净区 | 快速路径保留，找不到时才跑精确搜索兜底 |
| BUG-042-05 | 发布脚本从不看 jar：改了 Java 只跑打包那一半，包里是过时的 class 而检查全绿 | 新增闸门，重编 `plugin_src\*.java` 与 jar 逐 class 比对 |
| BUG-042-06 | 标注插件提示「已保存头向 3 条」而文件里只有 2 条（写盘前静默丢弃） | 写盘前全量校验，有问题一条都不写并逐个点名 |

逐条的现场、踩过的坑与判别性验证见 `AGENT.md` 第 8.8 节。验证规模：Python 159 条全过
（新增 17 条）、Java 16 条通过（新增 2 条）。其中 BUG-042-01 的 GUI 接线与 BUG-042-06 的
标注校验走界面那条路，**只有人工点得出来**，列在 `VALIDATION_0.4.2.md` 的待验收项里。

### 没有改的东西

- Python 推理路径、分割与 ROI 几何、界面行为：与 0.4.1 逐字相同，只有上面写明的两处
  （模型目录名、低清指纹集合）。
- **测量出来的数值**：`Area`、`Mean`、`Min`、`Max`、`IntDen`、`Median`、`RawIntDen`、
  `CTCF`、`RawCTCF`、`BackgroundCorrectedMean` 的计算式一个没动，ImageJ 测量本身也
  没动。变的是这些数写在**哪张表的第几列**，以及那张表放在**哪个文件夹**。
- 低清拆分（`low_clarity_split`）、形状细化、人工头向标注、人工排除区、部分圈画跳过：
  未动。
- 0.4.1 引入的半像素边界约定与那批 P1/P2/P3 修复：未动。

## 0.4.1

0.4.1 相对 0.4.0 只有一处实质改动：**导出的 ROI 改按像素边界画，ImageJ 量到的像素与圈虫统计的像素自此逐个相同。** 下面是这一版对用户有影响的几点。

- **修复了 ImageJ 比 Python 少算一圈边界像素**：ImageJ 判定像素是否属于 ROI，看的是像素**中心**落没落在多边形里，而导出的轮廓顶点原本落在像素**编号**上，两者差半格，于是每个方向少一行一列（实测同一只虫：Python 侧 45272 像素、ImageJ 侧 44325 像素，约 2.1%）。现在轮廓画在像素边界上，两边取到的像素集合逐像素相同。
- **数值与 0.4.0 及更早版本对不上，这是本次改动的目的**：用旧版导出的 ROI 比 Python 统计的区域小一圈，因此旧版跑、ImageJ 后端测出的面积、均值与 CTCF 会偏低（面积约差 2%；边界与内部亮度差得越多，均值差异越大）。**若要新旧数据混用，请以同一版本处理整批。**
- **代价**：ROI 不再抽稀，顶点数变多。同一批真实数据实测每张图的 ROI 总顶点数 1448 → 4690，`*_RoiSet.zip` 约 8.5 KB → 约 59 KB。
- **修复「窗口中临时设的校准被静默忽略」**：在 ImageJ 里用 `Analyze > Calibrate...` 或 `Analyze > Set Scale...` 设了校准、还没有存回文件时，原先的提醒不会出现（`ImagePlus.changes` 在这条路径上不会置位），而圈画与测量用的都是磁盘文件里的旧校准——屏幕是一张图，算的是另一套数。现在调用插件时会先比对窗口与文件的校准，逐项列出并说明采用文件上的那一份。
- **测量输出新增五列校准信息**：`PixelWidth`、`PixelHeight`、`SpatialUnit`、`ValueUnit`、`IntensityFunction`，附在每张图的 `*_measurements.csv` 与合并表末尾，逐行记录该图是用什么校准量出来的。
- **两项与手工测量有意不同的地方**（自 0.4.0 起如此，写在这里备查）：**不继承 `Limit to Threshold`**（插件统计整个 ROI），**不继承 `Redirect to`**（插件直接统计原图）。要与手工 `Analyze > Measure` 对上数，请先关掉这两项。

更早的 0.4.0 及其各轮复审见仓库历史中的 `CHANGELOG_0.4.1.md`（该文件已从发行包移除，全文只保留在仓库历史里）。
