# 自动圈虫 0.4.3 CUDA for ImageJ 验证记录

验证日期：2026-09-18

0.4.3 是**一次纯模型更新**：换掉随包的四个权重文件，另加一处行为改动（候选实例上限
12 → 32）。界面、输出目录结构、测量表列序、Java 插件与 0.4.2 逐字节相同，因此本文件只
验证模型与那一处上限改动。0.4.2 的输出结构改动不在本次范围内，其验证见
`VALIDATION_0.4.2.md`。

任何一处结论都是实际跑出来的，命令与原始输出一并写下。

## 结果

- Python 回归测试：**159/159 通过**，10 个模块全部 `OK`（本机无 pytest，逐文件直接运行）：

  | 模块 | 用例数 |
  |---|---|
  | `test_background_search.py` | 8 |
  | `test_boundary_training_033.py` | 4 |
  | `test_imagej_integration.py` | 62 |
  | `test_jar_source_check.py` | 9 |
  | `test_low_clarity_splitter.py` | 6 |
  | `test_make_release.py` | 17 |
  | `test_manual_exclusion.py` | 18 |
  | `test_manual_head_annotation.py` | 25 |
  | `test_review_policy_033.py` | 3 |
  | `test_worm_segment_selector.py` | 7 |

- Java 回归测试：`tests\AutoWormMeasurementTest.java` **16/16 通过**，打印的版本号是
  `自动圈虫 0.4.3`（证明这次改的 `VERSION` 真的进了编译出来的 class）。
- 高清留出集（4 张，模型没见过）：虫数全对 **4/4**（与 0.4.2 相同），平均 Dice
  0.9261 → **0.9302**，四项指标全部优于 0.4.2。
- 低清留出集（14 张）：虫数全对 10/14 → **11/14**，平均 Dice 0.8877 → **0.9081**。
  **有 2 张变差**，逐张列在下面，没有隐去。
- `补充样例5/test`（用户给的验收标准）：低清 **2/2 全对**，高清 1/2（`AWC-DR` 数出 18、
  真值 19）。高清这条短板的原因与决定见 `CHANGELOG_0.4.3.md`。
- 端到端回归（11 个老域的 100 张真实图，走完整条流水线）：**11 张虫数变化，5 张变好、
  6 张变差**。逐张列在下面。
- 低清指纹判据实测：两版低清权重（`0.2.2` 与 `0.2.1`）都判为低清，两版高清权重都不命中。
- 权重自带的验证指标与训练日志逐一核对一致；`image_size` 均为 768、`boundary_radius`
  均为 6。
- 发布文件已重建：`dist/Auto_Worm_ROI.jar`、`dist/AutoWormImageJ/AutoWormGUI.exe`。

## 训练数据的构成与留出划分

来自两个数据集的 `split_manifest.csv`，不是推算：

| 数据集 | 记录数 | 划分 | 来源前缀分布 |
|---|---|---|---|
| `training/high_v043` | 58 | 训练 54 / 留出 4 | `base` 12、`supp` 34、`supp4` 8、`supp5` 4 |
| `training/low_v043` | 108 | 训练 94 / 留出 14 | `base` 12、`boundary3` 20、`new` 30、`supp` 34、`supp4` 8、`supp5` 4 |

留出的仍然是 `supp4__0913-*-2` 这 4 张（高清、低清共用同一批，与 0.4.2 相同），以及低清
另外 10 张 `boundary3__*-2`。**留出划分与 0.4.2 完全一致**，因此两版的留出数字可以直接相比。

`supp5` 那 4 条就是 `补充样例5/train` 的四张（`ADF-DR n=17`、`AWC-DR+odor n=17`、
`N2-DR n=15`、`N2-DR+odor n=16`），在两个数据集里都**全在训练侧**——`补充样例5` 的验收图
是另放在 `test/` 里的两张，不在这个清单里。

高清随包权重仍是 `--train-all` 出来的（58 张全进训练，`trained_on_all_records: true`），
它自带的 `val_*_at_best` 是**在训练集上**算的；真正可比的泛化指标另起 `holdout_*` 前缀，
由单独跑的一版留出权重（`worm_unet_high043_holdout`，训练 54 / 留出 4）评测得到。
低清随包权重本身按 94/14 划出来（`trained_on_all_records: false`），它的验证集确实是留出的。

## 留出集对照（命令与原始数字）

评测脚本 `evaluate_worm_unet.py`，对每份权重跑：

```
python -X utf8 src/evaluate_worm_unet.py <dataset> \
    --checkpoint <权重> \
    --split-manifest <dataset>/split_manifest.csv \
    --only-split validation \
    --output <输出目录> \
    --erosion <N> --min-area <X> --min-height 0.10 --max-instances 32
```

> 后处理参数**必须显式传**：`evaluate_worm_unet.py` **不读** checkpoint 里的
> `postprocess_*`，它的默认值是 `erosion=0 / min_area=0.005`，与出模型时的设置不符。
> 高清用 `--erosion 2 --min-area 0.012`，低清用 `--erosion 3 --min-area 0.003`，两者都取自
> 各自 checkpoint 里的实际值。
>
> 0.4.3 起这里要多传 `--max-instances 32`：评估脚本的这个参数默认值是 12，与 0.4.2 的
> checkpoint 一致但与 0.4.3 的不一致，漏传会把所有 n>12 的图重新卡回 12 条，看起来像模型
> 没训好。**两版的对照都在 32 下重跑过**，否则 0.4.2 侧会被自己的上限压住，比较不成立。

### 高清 · 4 张留出图

| 图 | 0.4.2 (`0.1.1`) 虫数 | 0.4.3 (`0.1.2`) 虫数 | 0.4.2 Dice | 0.4.3 Dice |
|---|---|---|---|---|
| `supp4__0913-0-2` | 10 ✓ | 10 ✓ | 0.9254 | **0.9336** |
| `supp4__0913-10-2` | 10 ✓ | 10 ✓ | 0.9214 | **0.9256** |
| `supp4__0913-100-2` | 10 ✓ | 10 ✓ | 0.9289 | **0.9317** |
| `supp4__0913-1k-2` | 10 ✓ | 10 ✓ | 0.9286 | **0.9299** |
| **虫数全对** | **4/4** | **4/4** | | |
| 平均 Dice | | | 0.9261 | **0.9302** |
| 平均 tip Dice | | | 0.8662 | **0.8723** |
| 平均归一化端点误差 | | | 0.01943 | **0.01657** |

四张虫数都没变，四项平均指标全部改善，**没有一张变差**。端点误差从 0.0194 降到 0.0166，
是这次换 tip 精修器带来的（见下一节）。

### 低清 · 14 张留出图

| | 0.4.2 (`0.2.1`) | 0.4.3 (`0.2.2`) |
|---|---|---|
| 虫数全对 | 10/14 | **11/14** |
| 平均 Dice | 0.8877 | **0.9081** |
| 平均 tip Dice | 0.8110 | **0.8724** |
| 平均归一化端点误差 | 0.0335 | **0.0258** |

逐张（14 张全列，变好的标 ✓、变差的标 ✗）：

| 图 | 真值 | 0.4.2 | 0.4.3 | 0.4.2 Dice | 0.4.3 Dice |
|---|---|---|---|---|---|
| `boundary3__merge__g4__14__hsp4_020901_wdr5-2` | 10 | 9 | **10 ✓** | 0.726 | **0.856** |
| `boundary3__merge__g4__15__hsp4_250901_N2-2` | 10 | 10 ✓ | 10 ✓ | 0.890 | **0.901** |
| `boundary3__merge_split__g3__09__hsp4_250619_N2-2` | 9 | 10 | 10 | 0.897 | 0.896 |
| `boundary3__normal__g1__04__hsp4_250605_wdr5-2` | 10 | 10 ✓ | 11 ✗ | 0.884 | **0.894** |
| `boundary3__normal__g2__05__hsp4_250607_N2-2` | 10 | 10 ✓ | 10 ✓ | 0.920 | **0.922** |
| `boundary3__normal__g2__07__hsp4_250607_wdr5-2` | 10 | 10 ✓ | 10 ✓ | 0.917 | 0.916 |
| `boundary3__normal__g3__12__hsp4_250619_wdr5-2` | 10 | 11 | **10 ✓** | 0.899 | **0.918** |
| `boundary3__normal__g5__19__hsp6_250607_N2-2` | 10 | 10 ✓ | 11 ✗ | 0.934 | 0.924 |
| `boundary3__normal__g5__20__hsp6_250607_wdr5-2` | 10 | 10 ✓ | 10 ✓ | 0.885 | **0.896** |
| `boundary3__split__g1__03__hsp4_250605_N2-2` | 10 | 10 ✓ | 10 ✓ | 0.924 | 0.919 |
| `supp4__0913-0-2` | 10 | 9 | **10 ✓** | 0.800 | **0.914** |
| `supp4__0913-10-2` | 10 | 10 ✓ | 10 ✓ | 0.911 | 0.910 |
| `supp4__0913-100-2` | 10 | 10 ✓ | 10 ✓ | 0.914 | **0.919** |
| `supp4__0913-1k-2` | 10 | 10 ✓ | 10 ✓ | 0.927 | **0.931** |

**虫数：3 张变好、2 张变差，净 +1。** 变好的三张里两张是 `supp4__0913-0-2` 与
`boundary3__normal__g3__12`，都是从「差一条」改成全对；变差的两张
（`boundary3__normal__g1__04`、`g5__19`）两张都是 `boundary3`，也正是旧低清模型的训练域
——与 0.4.2 那一轮「加新域、让出旧域一部分」的规律一致，但这一轮让步更小。

值得单说 `boundary3__merge_split__g3__09`：真值 9，两版都数出 10，**这是唯一一张真值
不是 10 的留出图**，也是这次训练目标（n ≠ 10）在留出集上的直接检验点，**没有攻克**。它
是 `merge_split`（两条虫粘在一起又要分开），数的方向是多出来一条。这一版未处理，列出来
是因为它是本次目标下唯一没过的留出样本。

## `补充样例5/test` 验收

用户给的验收标准是同目录的 `answer.txt`。走的是**真实流水线**（`run_gui_batch`，形状细化
开、人工头向标注开、Python 测量后端、GUI 默认的 `standard_count=10` 预设），不是评测脚本：

```
high043: models/0.1.2/worm.pt + models/0.1.2/tip.pt   max_instances=32
low043 : models/0.2.2/worm.pt + models/0.2.2/tip.pt   max_instances=32
```

| 图 | 真值 | 高清 `0.1.2` | 低清 `0.2.2` |
|---|---|---|---|
| `ADF-DR+odor.tif` | 16 | 16 ✓ | 16 ✓ |
| `AWC-DR.tif` | 19 | 18 ✗ | **19 ✓** |

两张的 `qc_status` 都是 `REVIEW_COUNT_MISMATCH`、`expected=10` —— 这是**预期的**：GUI 默认
预设是每张十条虫，n=16/19 当然对不上，要用户在界面上按这批图的实际预期去调。`qc_status`
判的是「与预设是否一致」，不是「与真值是否一致」。

**低清 2/2 达标。** 高清差 `AWC-DR` 一张，原因见下。

### 高清这条短板

`AWC-DR` 是 2048×2048，而**高清域的三个样例全是 1024×1024**（样例1、补充样例1、
补充样例4），低清域的两个全是 2048×2048（补充样例2、补充样例3）。`补充样例5` 两张 test
图都是 2048²，本就属于低清域的尺寸。

把 2048² 的图缩到工作分辨率 768，相当于 2.67 倍下采样；高清模型只见过 1024²→768（1.33 倍），
挨得最近的两条虫之间那条缝在 2.67 倍下先糊掉，于是少圈一条。这不是训练轮数或数据量的问题
——同一个模型在 1024² 的老域上是 4/4 全对且指标全面优于 0.4.2（上一节）。

**试过一条别的路子，没用。** 既然两张 test 图都是虫体大量重叠的拥挤图
（`OverlapPixelsResolved` 3.2～4.5 万像素），一个自然的假设是「多出来的那条是被挤掉的，
该往重叠惩罚上调」。照这个假设另建了一份数据集（把 `补充样例5` 的四张显式打上 `merge`
标签，启用 `--contact-boost 3.0` / `--continuity-boost 1.75` / `--hard-repeat 4`），训出
`worm_unet_high043m`：它的选择分 0.8902 略高于随包那版 0.8884，但在同一套 test 上**还是
16 / 18，一张没多对**。因此这条短板不是重叠惩罚不足，**是尺寸/域的问题**，与上面那段
推断一致。这一版**没有采用**，随包仍是 `high_v043` 那一版。

**没有为这一条改高清模型**：为一张不属于高清域的图去调，会把它在自己域上的表现一起拖坏。
用户已知情并认可按现状收尾。

低清模型把两张都数对了（4 组 `补充样例5` 训练图 + 2 张 test 图共 6 张全对，起点 `0.2.1`
只对 4 张），这是这次训练目标下真正达标的那一个。

## 端到端回归（真实批次，新旧各跑一遍）

比 Dice 更能说明问题的是**端到端虫数**。脚本把 11 个老域的批次各自用新旧权重跑完整的
`run_gui_batch`，逐张对照 `batch_summary.csv` 的 `worm_count`。

| 批次 | 张数 | 模型档 | 虫数有变化的图 |
|---|---|---|---|
| 样例1（0716） | 8 | 高清 | 0/8 |
| 补充样例1（Pgst4） | 34 | 高清 | **2/34，全部变差** |
| 补充样例4（0913） | 8 | 高清 | 0/8 |
| 补充样例2（0114） | 30 | 低清 | **3/30（1 好 2 坏）** |
| 补充样例3（boundary3） | 20 | 低清 | **6/20（3 好 3 坏）** |

**合计 100 张，11 张有变化：5 张变好、6 张变差。**

变好的 5 张：

```
B_补充样例2 低清  Phsp16.2 0101-0-1 n=20   旧 12 -> 新 19   （真值 20）
               Phsp16.2 0101-50-1 n=20   旧 12 -> 新 20 ✓ （真值 20）
E_补充样例3 低清  ! hsp4 020901 wdr5-2     旧 9  -> 新 10 ✓
               ! hsp4 250901 wdr5-1      旧 11 -> 新 10 ✓
               hsp4 250619 wdr5-2        旧 11 -> 新 10 ✓
```

前两张正是被 `max_instances=12` 卡住的：0.4.2 在这两张真值 20 的图上两版都只能数出 12 条，
**12 就是那个上限本身**。抬到 32 之后一张数到 19、一张 20 全对。这也解释了 0.4.2 的
`VALIDATION` 里那句「`补充样例2` 里那 2 张 n=20 的图两版都是 12 条，属另一类问题」——
它不是另一类问题，就是这条上限。

变差的 6 张：

```
C_补充样例1 高清  Pgst4 0710-10-2 GFP       旧 10 ✓ -> 新 9
               Pgst4 Desgly D3 0529-1-2    旧 10 ✓ -> 新 9
D_补充样例2 低清  Phsp16.2 0101-90-2        旧 10 ✓ -> 新 11
E_补充样例3 低清  hsp4 250605 wdr5-2        旧 10 ✓ -> 新 11
               hsp4 250607 N2-1            旧 10 ✓ -> 新 11
               hsp6 250607 N2-2            旧 10 ✓ -> 新 11
```

**这 6 张是这一版的代价，如实记下。** 分两类：

- **低清域 4 张**（`Phsp16.2 0101-90-2` + `boundary3` 那 3 张）：`补充样例5` 本就是低清域，
  微调时模型多出来的候选带到了同域的老图上。方向都是「多一条」。
- **高清域 2 张，且是在高清自己的老域 `补充样例1` 上**：高清的 `0.1.2` 在那批 34 张里丢 2 张、
  一张没多，方向都是「少一条」。这是本轮最需要知道的一条 —— 高清模型为了新数据让出了老域
  的 2 张，虽然它在另一批高清老域（样例1 · 0716 与 补充样例4 · 0913）上 16 张一张没变。

**净账是 5 好 6 坏。** 用户在同一轮的决策里已知情：主要目标是 n ≠ 10，低清 test 2/2 达标，
高清这条短板按现状记进文档。**这不等于「全面提升」，也不等于「只赚不赔」。**

## 两个 tip 精修器

整虫模型换了，精修器的训练输入（guide）必须跟着重出，否则它学的是**旧模型犯的错**。流程
与 0.4.2 相同：先用新整虫模型对各自数据集的全量记录跑 `evaluate_worm_unet.py`（不加
`--only-split`，因此训练集和验证集都有），断言每张都产出了 `_predicted_instances.png`
（精修器的 `TipDataset` 就是按这个名字找 guide 的），再以它为输入训练。

超参不传任何值，用脚本默认（epochs 50 / batch 16 / lr 3e-4 / patch 192 /
`probability_threshold` 0.40 / `replace_fraction` 0.14），与随包 tip 的元数据逐项相同；
只传 `--train-all --initial-checkpoint --output-name`。

| | 0.4.2 | 0.4.3 | 最优点 |
|---|---|---|---|
| 高清 tip | 0.9380 | **0.9523** | 第 46 / 50 轮 |
| 低清 tip | 0.9347 | **0.9509** | 第 49 / 50 轮 |

两个都是 50 轮跑满、取过程中 `val_patch_dice` 的最高点，起点（第 1 轮）都低于最终值。
整虫模型换了之后两端都涨了约 0.014～0.016，与留出集上 tip Dice 的改善方向一致。

## 权重元数据核对

逐键读出新旧四个权重，确认没有「改了一个没注意到的数」：

| 键 | 0.4.2 `0.1.1` | 0.4.3 `0.1.2` | 0.4.2 `0.2.1` | 0.4.3 `0.2.2` |
|---|---|---|---|---|
| `image_size` | 768 | 768 | 768 | 768 |
| `boundary_radius` | 6 | 6 | 6 | 6 |
| `normalization_mode` | `legacy` | `legacy` | `background_aware` | `background_aware` |
| `postprocess_erosion` | 2 | 2 | 3 | 3 |
| `postprocess_min_area` | 0.012 | 0.012 | 0.003 | 0.003 |
| `postprocess_min_height` | 0.1 | 0.1 | 0.1 | 0.1 |
| **`postprocess_max_instances`** | **12** | **32** | **12** | **32** |
| `tip_boost` / `tip_fraction` | 4.0 / 0.18 | 4.0 / 0.18 | 1.0 / 0.18 | 1.0 / 0.18 |
| `hard_repeat` / `contact_boost` / `continuity_boost` | 1 / 1.0 / 1.0 | 1 / 1.0 / 1.0 | 4 / 3.0 / 1.75 | 4 / 3.0 / 1.75 |
| `base` | 16 | 16 | 16 | 16 |
| `trained_on_all_records` | `True` | `True` | `False` | `False` |
| `initialized_from` | `models/0.1.0/worm.pt` | `models/0.1.1/worm.pt` | `models/0.2.0/worm.pt` | `models/0.2.1/worm.pt` |
| `best_selection_score` | 0.8866 | 0.8884 | 0.8046 | 0.8061 |

**只有 `postprocess_max_instances` 一项是有意改的（12 → 32），其余全部不变。** 两端推理侧
`batch_worm_roi.py` 的默认值仍是 12（`checkpoint.get("postprocess_max_instances", 12)`），
因此这个值完全由权重决定，不会出现「权重说 32、代码用 12」的错配。

`initialized_from` 是「从哪一版权重微调来的」的历史指针，不是运行时路径，代码里没有任何
地方读它；四个值都写成仓库内相对路径，没有开发机绝对路径。

### 训练日志（选点依据）

```
高清 train-all   Best validation selection score 0.8884   （0.4.2 为 0.8866）
                 epoch 37/40 val_fg=0.9680 val_interior=0.9172 val_boundary=0.8075
                             val_tip=0.9411 score=0.8884

高清 留出权重     Best validation selection score 0.8798
                 epoch 3/40  val_fg=0.9628 val_interior=0.9075 val_boundary=0.7941
                             val_tip=0.9375 score=0.8798

低清 94/14       Best validation selection score 0.8061   （0.4.2 为 0.8046）
                 epoch 5/40  val_fg=0.9529 val_interior=0.8811 val_boundary=0.7658
                             val_tip=0.9529 score=0.8061
```

高清留出权重的最优点落在第 3 轮，说明 40 轮的训练里模型很快就到顶、之后是过拟合段 ——
但它的作用只是产出 `holdout_*` 那四个泛化指标，**不进发行包**，随包的是 train-all 那一版，
因此这个早停不构成问题。低清同理，第 5 轮最优点，随后刻意继续跑到 40 轮以便留出 `val_*`
的完整曲线，选点脚本取的是历史最优点而不是末轮。

三份日志的 `selection_score` 与各自 checkpoint 里的 `best_selection_score` 逐位相同。

## 低清指纹判据

`batch_worm_roi.py` 的 `LOW_CLARITY_MODEL_SHA256S` 实测（导入的是本仓库 `src/`）：

```
常数： ('dd5e6edc9485cbc5789870ec0bf78ac1578b5917210e1b6aab61158432eebb1a',   # 0.2.2（0.4.3）
        'afb80dc4bde0cc644ab4269bfc73463d7f20eace9592a21857ce155b8f4eed4b',   # 0.2.1（0.4.2）
        '5ce04d283a8dbc4e7b8ae460f6310627ecdfbef5feca23ace7bdbe51ef84ff3a')   # 0.2.0（0.3.3~0.4.1）
SOFTWARE_VERSION = 0.4.3-beta-imagej

models/0.2.2/worm.pt   dd5e6edc9485cbc5...  判定为低清 = True
models/0.2.1/worm.pt   afb80dc4bde0cc64...  判定为低清 = True
models/0.1.1/worm.pt   6315cfaf21594c93...  判定为低清 = False
models/0.1.2/worm.pt   13e7072b43591e34...  判定为低清 = False
```

四个方向全部符合预期：两版低清都认得出，两版高清都不命中。**这一版把新指纹加在了最前面，
旧的两个保留** —— 0.4.2 与 0.4.1 散出去的 `0.2.1` / `0.2.0` 用户还在用，只认新的话他们走
命令行时会被静默当成高清，正是这条判据要防的事。这条只在**命令行**路径上生效（界面每次都
把 `low_clarity_split` 显式传进来）。

## 输出目录结构与测量表列序

**本次未改动。** 0.4.3 没有碰 `measurements/` + `other/` 的布局，也没有碰测量表的列序，
相关验证沿用 `VALIDATION_0.4.2.md`。本次 Python 测试里 `test_imagej_integration.py` 的
62 条（0.4.2 为 60 条）覆盖的仍是那套布局，全部通过。

## 发布文件

```
dist/Auto_Worm_ROI.jar                            24413 字节
  sha256 8bd27a888484e27d00d3f06de7ca7b7dfd11c055fdaee0c3f9a65df739b08daa
dist/AutoWormImageJ/AutoWormGUI.exe               47962825 字节
  sha256 47002e473c06846fc635b36584d2fbece2303c41cad8fbb9423569917926aec1

models/0.1.2/worm.pt  13e7072b43591e34d0d7dc1986f5f5553b5642f51f7aa68b91206f174fa479e7
models/0.1.2/tip.pt   ecfaeaaa8d3b0bcbb13cd862601b55ee1713017e28d511fd6c2b633de3759bea
models/0.2.2/worm.pt  dd5e6edc9485cbc5789870ec0bf78ac1578b5917210e1b6aab61158432eebb1a
models/0.2.2/tip.pt   076c0adf4aa8a9d0b2ca8baed24256a7f57cdf5302aca154f827c2527cd0261a
```

`dist/AutoWormImageJ/models/` 下**八个**文件（四个目录）的 SHA256 与上表及仓库 `models/`
**逐个相同**（`postbuild.py` 整棵 `models/` 复制）。

> **这一版包里带了四个模型目录（`0.1.1` / `0.1.2` / `0.2.1` / `0.2.2`），而不是只有当前
> 一对。** 0.4.2 是手工只放了当前一对；这一版是构建脚本的自然结果，没有做裁剪。多出来约
> 24 MB，不影响运行时行为（`MODEL_CONFIGS` 指向 `0.1.2` / `0.2.2`）。故意不删：删掉的话
> 任何一次重建都会把它们带回来，包的内容就不再等于构建脚本的输出 —— 那种「手工对不上脚本」
> 的状态正是 0.4.2 那轮外部审查抓 BUG-042-05 时想避免的。

`plugin_src/plugins.config` 与 `src/Auto_Worm_ROI.java` 里的版本字符串都已改成 0.4.3，实测
解压 jar 读出的第一行是：

```
Plugins>Auto Worm ROI, "Auto Worm ROI 0.4.3 beta", Auto_Worm_ROI
```

> **jar 的 SHA256 只对得上「这一个文件」。** `jar` 会把每个 class 的打包时刻写进条目头，
> 同样的源码重编一次、哈希就变一次。要判断手上这个 jar 是不是这一版，**别看哈希，看内容**：
> 解压 `plugins.config` 看版本，或跑 `make_release.py`（它会重编 `plugin_src\*.java` 与 jar
> 里的 class 逐个比对）。EXE 同理，PyInstaller 产物每次构建也不同。

## 未做自动验证的部分

- **人工界面的验收**：本次只动了模型文件与版本字符串，界面代码一行没改；但换了随包权重，
  界面上分别选「高清晰度图像 / 低清晰度图像」各跑一张真实数据，仍建议人工过一遍。
- **`补充样例5` 的 4 张训练图不能作为依据**：它们在训练集里。用户在 `test/` 上的验收才是
  独立检验点，这也是本文件把 test 那一节单列的原因。
- **低清留出集上 2 张变差的图**（`g1__04`、`g5__19`）与**端到端回归里 6 张变差的图**均未做
  进一步处理或补偿。
- **`boundary3__merge_split__g3__09`（真值 9）两版都数出 10**，未处理。它是留出集里唯一一张
  真值不是 10 的图，也就是本次训练目标在留出集上的唯一直接检验点，**没有通过**。
- 高清在 2048² 输入上的表现没有系统评测过：本次只在 `补充样例5/test` 的 2 张 2048² 图上
  看到「差一条」，没有做更大规模的 2048² 高清评测来量化。**这是已知的度量缺口。**

## 尚需人工界面验收

本次沿用 0.4.2 与 0.4.1 遗留的界面验收事项，未逐条重跑（涉及的都是界面在各版本间没变过的
行为），跑一次即可同时覆盖：安装/首次启动能正常打开、选完输入输出目录后能开跑、跑完弹出的
结果目录正确、界面上的「高清晰度图像 / 低清晰度图像」两个选项各自能跑到出结果、批处理中途
点取消能停下、以及输出目录里不再残留临时文件。

本次新增的一项：

- **把 `补充样例5/test` 的两张图在界面上各跑一遍**（高清档、低清档都跑），确认界面给的虫数
  与本文档「`补充样例5/test` 验收」一节一致：低清两张 16 / 19，高清 16 / 18。界面默认预设
  是每张十条虫，两张都会显示「需人工复核」——这是对的，不是故障。

  > 这里**不能**靠界面文案来确认用的是哪一版权重：`MODEL_CONFIGS` 里的 `version` 字段
  > （`0.1.2` / `0.2.2`）**没有任何代码读它**，界面上不会显示。要确认版本，看
  > `dist\AutoWormImageJ\models\` 下实际存在哪个目录，或对权重文件算 SHA256 与本文档的
  > 字表对照。函数 `_validate_runtime`（`src/worm_roi_gui.py`）只检查 `checkpoint` /
  > `tip_checkpoint` 两个文件**存不存在**，不校验版本。
