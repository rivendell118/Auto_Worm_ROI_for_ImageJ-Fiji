# 0.4.1 CUDA for ImageJ

## 0.4.1（2026-09-15）

这一版只比 0.4.0 多一处改动：**导出的 ROI 改按像素边界画，ImageJ 量到的像素与圈虫统计的像素自此逐个相同。**

- **修复 ImageJ 后端比 Python 后端少算一圈边界像素**：ImageJ 判定一个像素属不属于 ROI，看的是像素**中心**落没落在多边形里（像素 `(c, r)` 的中心就在 `(c, r)`）；而被导出的轮廓来自 `cv2.findContours`，顶点落在像素**编号**上。两者的取样点差半格，于是边界那一圈像素的中心恰好落在多边形边上，被判在外——**每个方向少一行一列**。实测同一只虫：Python 侧 45272 像素、ImageJ 侧 44325 像素，约 2.1%。现在轮廓改画在像素边界上（第 `c` 列的左右边界在 `c-0.5` / `c+0.5`，第 `r` 行的上下边界在 `r-0.5` / `r+0.5`），顶点落在半整数上，两边取到的像素集合逐像素相同；背景矩形同样处理。端到端复核：三张真实样例各 10 条虫 + 1 个背景，共 44 个 ROI，ImageJ 的 `pixelCount` 与 Python 写进 `*_measurements.csv` 的 `Area` **全部相等**（含 172203 像素的背景与 47554 像素的虫体）；带人工排除区的那条虫仍导出为复合 ROI，洞照旧被挖掉，Python 侧 45097 = ImageJ 侧 45097。
- **数值会对不上历史数据，这是本次改动的目的**：0.4.0 及更早版本导出的 ROI 比 Python 统计的区域小一圈，因此同一批图用旧版跑、用 ImageJ 后端测出的面积、均值与 CTCF 会比 0.4.1 低（面积约差 2%；边界与内部亮度差得越多，均值差异越大）。Python 后端的输出不受影响——它一直就是这个约定。若要新旧数据混用，请以同一版本处理整批。
- **代价**：ROI 不再抽稀（抽稀会破坏这套约定），顶点数变多。同一批真实数据实测：每张图的 ROI 总顶点数 1448 → 4690，`*_RoiSet.zip` 约 8.5 KB → 约 59 KB。单张 59 KB 对 Fiji 不构成负担。
- **修复「窗口中临时设的校准被静默忽略」**（外部定量审查 [P2]）：调用插件时的提醒只检查 `ImagePlus.changes`，而 `Analyze > Calibrate` 走的是声明 `NO_CHANGES` 的滤镜、`ImagePlus.setCalibration()` 也只是把 `Calibration` 存下并指回图像，两者都不会置位这个标志（已用 `javap` 核对 `ij.jar` 的字节码确认）。于是在窗口里设了局部强度校准、没有保存回 TIFF 就调用插件时，提醒不会出现；而原界面圈画和随后的测量都只针对磁盘上的文件，用的是文件里的旧校准——屏幕是一张图，算的是另一套数。审查给出的复现（16 位图，虫体 100 像素、每像素 100，背景每像素 20，窗口设 y = 10 + 2x）在本机重跑，得到的是同一个结果：窗口 CTCF **16000**，插件按磁盘文件测 **8000**，而 `changes` 全程为 `false`。现在 `execute()` 在启动原界面之前增加一次校准比对（`confirmSameCalibration` + `calibrationDifference`）：把窗口与磁盘文件各自的校准逐项列出（像素宽高与空间单位、强度函数与系数与强度单位），点「确定」继续即用文件里的校准，点「取消」则什么都不做、状态栏写明未处理。该检查直接以文件为基准，不经过 `changes`；未保存但改了像素的情形仍由原有的 `changes` 提示负责，两者互不替代。
- **测量输出新增五列校准信息**：`PixelWidth`、`PixelHeight`、`SpatialUnit`、`ValueUnit`、`IntensityFunction`，附在每张图的 `*_measurements.csv` 与合并表末尾，逐行记录该图是用什么校准量出来的（例如 `0.500,0.500,micron,Gray,Straight Line: y = 10 + 2*x`）。此前表里只有 `MeasurementBackend`，两张用不同校准量出的图在事后无法分辨。同时每批开始测量时在日志里写一行「本次测量使用的校准（首张图 …）」，全局标定生效时一并写明——`ImageJ` 的全局校准会覆盖各图自己的校准（`ImagePlus.getCalibration()` 在全局标定存在时返回全局的那一份），同一批图换台机器跑出不同数值时，这行日志是唯一能解释原因的地方。窗口与文件校准不一致时也会在日志里写明，仍按文件上的校准测量。
- **`RawIntDen` 仍是真正的原始像素和，没有为了与 ImageJ 同名列一致而改成校准后的值**：审查实测 16 位强度校准图（y = 10 + 2x，ROI 内 100 像素、每像素原始值 100）时，插件 `RawIntDen = 10000`，而本机 ImageJ 1.54p 同名列是 `21000`——后者算的是 `pixelCount × umean`，该版本的 `ShortStatistics` 在有强度校准时把 `umean` 赋成了**校准后**的均值。插件取的是 `getRawStatistics()`（内部以空校准调用 `ImageStatistics`），得到的确实是原始像素之和。**这一列在插件里只服务于 `RawCTCF`**：`RawCTCF` 就是接入 ImageJ 之前那套算法算出的 CTCF（`raw_sum − 像素数 × 背景原始均值`），按既有约定保留作参考，除此之外表里没有任何列或下游脚本读它（Python 侧的原始和校验读的是手工 ImageJ 的 `Results_*.csv`，不是本插件的输出）。因此这一列与 ImageJ 1.54p 同名列在「有强度校准」这一种情形下数值不同，是**同名不同义**，不是算错；`CTCF` 一项两边一致。若将来要与 ImageJ 的这一列逐位一致，必须同时改 `RawCTCF` 的语义并重新核对既有数据，不能只改列名下的算式。
- **两项有意保持、需要在文档里写明的兼容性差异**：① **不继承 `Limit to Threshold`**——插件的测量选项是固定的（`AREA | MEAN | MIN_MAX | INTEGRATED_DENSITY | MEDIAN`，不含 `LIMIT`），因此统计整个 ROI；手工 `Analyze > Measure` 若勾了该选项只统计 ROI 内符合阈值的像素，两边面积、均值、背景均值与 CTCF 都可能不同（审查给出的合成例子：插件 Area 600 对限阈值 312）。按完整 ROI 测量是 0.4.0 起的既定行为，不是缺陷；对照手工测量时请关闭该选项。② **不继承 `Redirect To`**——插件直接统计原图，这是 0.4.0 修掉「结果行标原图名、实际量到另一张图」之后定下的设计，无需为「与手工一致」而撤销。两处都会写进随包 README 的适用条件一节。
- 单元测试数 110 → 112：`tests\test_manual_exclusion.py` 新增两项，断言「导出的 ROI 按 ImageJ 的采样规则读回来 == Python 掩膜」，其中之一带 2 个孔洞、并把 ROI 写盘后读回再验一遍；该文件原先用 `cv2.fillPoly` 栅格化多边形的辅助函数一并改为按 ImageJ 的规则采样（像素中心 + 非零填充），否则测试盯的仍是旧约定。Java 侧 `tests\AutoWormMeasurementTest.java` 3 → 5 项，新增「每行都记录该图的校准」与「窗口里设的校准会被认出是差异」两项；后者同时断言该夹具下 `ImagePlus.changes` 仍为 `false`——这正是需要一个新判据的原因，若哪天 ImageJ 改了行为、该夹具不再复现静默情形，这条断言会失败并提醒改夹具。本轮**未改 Python 代码**，`AutoWormGUI.exe` 未重建、哈希不变；jar 因 Java 改动重建，但插件行为除上面两处新增外没有变化（原有测量路径与数值语义未动）。
- 人工验收新增一条：在带界面的 Fiji 里确认 ROI 轮廓正好压住虫体最外圈像素的外沿，且 `Measure` 出的 `Area` 与 `*_measurements.csv` 里同标签的 `Area` 相等。详见 `VALIDATION_0.4.1.md`「尚需人工界面验收」第 6 条。

以下三条来自随后一轮的 Bug 复审（一份外部复审 + 一份本机自查，合并见 `0.4.1_综合Bug复审.md`）。复审共 13 条，均**不是** 0.4.1 引入的回归，而是自 0.4.0 起就有的历史行为；当时只修其中 P1 里的三条（P1-1、P1-3、P1-4），余下 1 条 P1 与 P2/P3 共十条留待后续。三条的共同点是**出错了却不报错**。

### 第三轮：P1-2、P2-5 ~ P2-8、P3-10、P3-11（同上一份复审，余下条目里可改代码的七条）

复审余下的十条里，七条改代码（下面逐条说明），三条（P3-9、P3-12、P3-13）属于「写明限制、给出处置办法」，只改随包 README，见本节末尾。

- **修复「离线校验工具按另一套光栅化规则数像素」**（P1-2，`src\inspect_roi_dataset.py`）：0.4.1 修好了**导出**侧的半像素约定（轮廓画在像素边界上），但**读**回来做校验的那个工具没有跟着改，用的还是 `PIL.ImageDraw.polygon` 的「按像素格填充」。两套约定在边界上正好差一圈：一个 `n × n` 的方块按格填充会填出 `(n+1) × (n+1)` 个像素，因此这个工具会把正确的数据报成面积偏大约 6%——**方向正好相反**，一个专门用来「证明 ROI 和掩膜对得上」的工具反而在误导人。现在改为与 ImageJ 相同的规则：对每个像素中心求多边形绕数，按非零填充规则取值；孔洞是反向绕行的子路径，将来若要读复合 ROI 也仍然正确。判据与 `tests\test_manual_exclusion.py` 里那份独立实现一致，两边互为对照。
- **修正像素掩膜的数组形状**：上面那次改写一开始把累加器写成 `np.zeros(size)`，而 `size` 是 PIL 的 `(宽, 高)`，其余按行列索引的部分用的是 `(高, 宽)`——**两者只在正方形图上相等**。非正方形图会直接以广播失败告终。这是上面那条的回归测试（含 40×60 与 12×9 两种形状）当场发现的，已改为按行列数建累加器。**没有进入任何已发布版本。**
- **修复「有图没有虫时，整次离线校验停在中途」**（P2-8）：圈虫给每张图都追加一个背景 ROI，因此模型没找到虫的图有且仅有 1 个 ROI、虫列表为空。校验器对虫列表取 `min()`/`max()`，空序列直接抛 `min() iterable argument is empty`；这个调用当时没有护栏，于是**扫描就此结束**，后面每一张图的报告都没了——真正异常的那张图，成了整个检查停下来不干的原因。现在空列表记 `nan` 照常成行，并对每张图单独兜底：一张图读不出 ROI 或对不上表格时，只打印这一张的失败原因，其余照常。失败的那张仍会点出文件名与原因，不会变成静默少一行。
- **修复「停止批处理后，已完成图像的 ROI 被丢掉、不交给 ImageJ 测量」**（P2-5）：`run_gui_batch` 有意保留已完成图像的 ROI 集与汇总行（随包 README 就是这么写的：中途取消也能拿到已完成的部分），但界面在用户停止时一律发 `cancelled`，而插件对 `cancelled` **不做任何测量**。结果是一批被中途停下的图里，凡是已经跑完的，ROI 集在输出文件夹里躺着、荧光和 CTCF 却一个都没有，且没有任何提示。现在停止时改为发送 `complete`，并把成功清单收窄到**界面确实见到完成**的那几张（依据进度日志行收集的 `completed_stems`）；只有一张都没跑完时才仍发 `cancelled`。
- **修复「复用输出目录时，上一轮的单图测量表冒充本轮结果」**（P2-6）：合并表 `ImageJ_measurements_all.csv` 一直是测量前先删掉的，单图那张 `<图名>_measurements.csv` 却从来不删。于是本轮该图测量失败时（ROI zip 被截断、文件不再是可读 TIFF），上一轮留下的那张表**原地不动**：日志说这张图已跳过、新合并表里也没有它，可打开文件夹看，一张写着这张图名字的测量表就在那里，旁边是本轮新写的 ROI。**表里的数值是真的，只是来自已经被重新分割过的数据**，而文件里没有任何东西说明这一点。现在插件在测量每张图之前先删掉自己上一轮写的表，识别方式与它识别合并表时相同——按两个列名（`CTCF` 与 `MeasurementBackend`）判断而不比对整行表头，因此 0.4.0 以来任何一版的表（含 0.4.1 新增的五列校准）都认得出来；只删自己写的，输出目录里同名但不是本插件写的文件一律不动（有回归测试）。Python 侧同样补上：用 ImageJ 后端跑的时候，输出目录里那份属于插件的旧表（两种后端共用这个文件名、schema 不同）也会先删掉。
- **修复「选好文件夹后它已不可读，点开始没有任何反应」**（P2-7）：开始处理的路径上，列输入文件夹是**唯一没有护栏**的文件系统调用，而它排在整套预检流程之后。文件夹被删除/改名、U 盘拔掉、网络共享权限被收回时，异常直接逃出回调——带窗口的发行版没有标准错误可以打，于是按钮像是坏的：没有任何提示，前面的预检也都白跑了。现在捕 `OSError` 并明确提示重新选择文件夹，同时把系统给的原因一并写出。
- **修复「已保存的空间校准被误判成窗口与文件不一致」**（P3-10）：比较像素尺寸用的是 double 的**精确不等**，而 TIFF 保存空间校准会发生有限精度量化。实测把窗口里的 `0.123456789` 存盘再读回，得到的是 `0.1234567901234568`（两值均为本机重跑所得，与复审给的一致），于是每张这样的图都被报成「窗口与磁盘的校准不同」，用户只能一次次点「确定」，没有任何办法让它停下来（该提示**不影响数值**，只写日志/弹窗）。现在像素宽高按**相对容差 1e-6** 比较（远粗于上述量化，远细于任何人为设定的尺度差别：容差内的差别在第 6 位有效数字之后），单位仍按原样比较；同时**不再比较 `pixelDepth`**——它是切片间距，本轮所有测量（单平面上的 `Area`、`Mean`、`CTCF`）都看不到它，拿它触发提示只是多一个无法处置的弹窗。
- **修复「关掉低清晰度拆分后重跑，旧的拆分报告留在输出目录里」**（P3-11）：`_split_qc.csv` 的写分支没有 `else`，而它的三个同类（平滑修复、人工标注、部分圈画）都有——功能关闭时顺手删掉上一轮的同名文件。缺这一条意味着输出目录里会出现一份描述「本轮没有执行的步骤」的报告，且文件里没有任何东西说明它是哪一轮写的。现已补齐，写出的表头与读取时用来识别的是同一份字段定义。
- 测试：Python **115 → 131**（新增 16 项：插件单图表清理 5、`_split_qc.csv` 清理 3、掩膜读取往返 3、离线校验扫描 2、界面三条 3）；Java `tests\AutoWormMeasurementTest.java` **6 → 12**（新增「上一轮的单图表会被删」「失败图不留下单图表」「只像我们表的文件不动」「本轮没走到的图保留其表」「被量化的像素尺寸不算差异」「真实 TIFF 存读往返不算差异」）。P2-6 与 P3-10 另用一份独立探针在**上一版 jar**（`SHA256 64CDF731…`，即 P1 三条修复后、本轮修复前的那一只，从上一个发行包里取出）与本次重建的 jar 上各跑一遍：P3-10 由 `difference=REPORTED` 变为 `none`，P2-6 由 `stale table still there = true` 变为 `false`。逐字输出见 `VALIDATION_0.4.1.md` 本节。**这一轮 Java 与 Python 都改了**，`Auto_Worm_ROI.jar` 与 `AutoWormGUI.exe` 两件都已重建。

### 本轮只写入 README 的三条（不改代码）

复审里的 P3-9、P3-12、P3-13 属于「限制或时序问题，写清楚就能绕开」，本轮按这个方式处理，改动只在随包 README：

- **文件夹名里带 `[` `]` 会让整批被拒**（P3-9）：列目录那一步用通配匹配，方括号在通配语法里是字符集，`[batch2]` 被当成「6 个字符里的任意一个」，于是一张图都匹配不到、整批报 `No TIFF images found`，而界面上明明列着图（界面用的是另一种列目录方式），现象与信息对不上。README 新增「文件与文件夹命名」一节写明这一点，并建议图像文件名里也不要用方括号。**代码里的一行修法（转义路径）本轮未做**，留待后续。
- **文件名首尾带空格会被静默跳过**（P3-13）：测量环节拿文件名字符串与批处理报上来的名字逐字比较，而界面写成功清单时做了 `strip()`，首尾多一个空格就匹配不上——那张图既不测量也不报错。同上一并写进 README。**同样未改代码**（两侧的契约要一起改，属于协议改动）。
- **退出请求被模态框吞掉 / 桥接残留永久挡住「开始」**（P3-12）：前者是「点了 ✕ 窗口没关、状态栏停在等待写出」，不丢数据，再点一次即可；后者是测量途中直接关掉 ImageJ 时，桥接目录里残留的通知文件会让之后每次点「开始处理」都弹「ImageJ 正在测量上一批结果」，界面里没有清除的办法，需要关掉界面窗口、重开 Fiji 再运行插件（或手工删掉 `%TEMP%\autoworm-imagej-*\` 里的残留文件）。两条的处置办法都写进了 README 的「常见问题」。

- **修复「单图测量表写失败时，它的数据仍然进了合并表」**：测量逐图进行，每图有自己的 `*_measurements.csv`，全部测完后汇总成 `ImageJ_measurements_all.csv`。原先是一边测一边把行加进合并表，单图那张表则在循环里单独保存；于是单图那张表写失败时（磁盘满、权限、文件被占用），日志把这张图记成「已跳过」，可它的行已经留在合并表里了。**合并表里那几行数值是真的**，这使问题很难被发现：输出目录里再没有别的东西能与之矛盾，而旁边那张不存在的单图表容易被读成「这张图不必单独出表」。现在顺序反过来——先把该图的行攒起来，**单图表写成功落盘之后**再折进合并表；写失败则该图按跳过处理，合并表里没有它，日志与目录内容一致。回归测试用「在表的位置放一个目录」触发（测量本身成功、只有写失败），并额外断言占位目录及其中的文件没有被替换掉。
- **两张测量表改为原子写，并因此发现 `ResultsTable.saveAs()` 是按扩展名选分隔符的**：保存改为先写同目录下的临时文件再原子改名（`Files.move` + `ATOMIC_MOVE`，不支持时退回普通 `REPLACE_EXISTING`；失败时删掉临时文件再抛）。临时文件的命名一开始写成 `<名>.csv.tmp`，结果**这批测量表全部变成制表符分隔**——`ResultsTable.saveAs()` 只在文件名以 `.csv` 或 `.CSV` 结尾时才写逗号，否则写 TAB（已反汇编 `ij/measure/ResultsTable.class` 确认）。改成 `<名>.tmp.csv`（扩展名放最后，与 Python 侧 `_atomic_write` 的命名一致）后恢复。**这一条值得单独记下**：表本身是实验数据，用 Excel 打开时逗号与制表符看不出区别，只有按逗号解析的下游脚本会整列错位。
- **修复「用 Excel 保存过一次的 QC 汇总表会让窗口再也打不开」**：界面读取 `_qc_status_summary.csv` 时捕的异常是 `(OSError, csv.Error)`，而 `UnicodeDecodeError` 是 `ValueError` 的子类、不是 `OSError`，且在逐行迭代时才抛出。这个文件是质检流程自己产生、README 又让实验员去看的，在中文机器上用 Excel 打开再保存一次就会变成 GBK 编码，于是异常逃出；而**文件夹路径是被记住的**，此后每次启动窗口都在恢复上次文件夹时倒下——用户看到的是「程序一开就崩」，只能靠改文件名恢复。同类读取器在批处理侧一直捕了这个异常，只有界面这一处漏了。现在补上，行为回到一致：读不出来就当没有汇总表，界面照常打开。
- **修复「界面的事件泵一旦抛异常就永久停摆」**：界面靠每次处理完消息队列后重新挂一个延时回调来继续跑。原先这次重新挂接写在异常处理之外、函数末尾，于是处理某条消息时只要抛出任何异常，异常逃出回调（Tk 只把它打到标准错误），**重新挂接那一步也不会执行**，事件泵从此再不被唤醒。表现出来是「跑完了但界面不动」：进度不更新、按钮不回弹、状态栏停在最后一句。现在用一个「是否继续」的标志，在 `finally` 里按它重新挂接，并让意料之外的异常在终端面板留一行中文提示加堆栈，把「安静地停摆」变成「界面照常运行、日志里有话」。**窗口已销毁时不能重新挂接**（整批处理结束时窗口会被关掉），这一条由返回值区分，不是简单的 `finally`。
- 单元测试数 112 → 115（新增三项：另一编码的汇总表不会挡住窗口、坏汇总表不会让一批停在中途、事件泵出错后仍继续运行）；Java `tests\AutoWormMeasurementTest.java` 5 → 6（新增「无法写出单图表的图像不在合并表里」一项）。P1-1 所盯的行为另用一份独立探针在**冻结的 0.4.0 jar** 与本次重建的 jar 上各跑一遍作对照：改动前 `blockedRowInCombined=true`、改动后 `false`，且改动前整批**并不报错**（`throw=none`）——「被跳过的图仍在合并表里」与「没有任何东西提示这件事」同时成立。逐字输出见 `VALIDATION_0.4.1.md` 本节。**这一轮 Java 与 Python 都改了**，因此 `Auto_Worm_ROI.jar` 与 `AutoWormGUI.exe` **两件都已重建**（此前一轮是「只重建 jar、EXE 哈希不变」）。

### 安装后实测发现：带人工排除区的图会整张失败（只读数组）

0.4.1 装到本机 Fiji 后，用户拿真实数据跑出来一处崩溃：`0913 tif green` 那批 8 张图里，`0913-100-2 green.tif` 记成 `FAILED`，错误是 `ValueError: assignment destination is read-only`，其余 7 张正常。

- **两个条件同时成立才会崩**：① 界面里「形状细化」开着（`predict_raw` 因此被要求返回前景概率）；② 这张图上有**人工排除区**。那张图恰好是 8 张里唯一带排除区的（1 个排除区、0 箭头、0 边界线）；同批另一张有 10 个箭头 + 18 条边界线、但没有排除区的图正常跑完，所以看上去像是「排除区」而不是「手动标注」的问题。
- **根因**（`src\batch_worm_roi.py` 的 `predict_raw`）：前景概率是经 PIL 缩放拿回来的，而 `np.asarray()` 会把 PIL 缓冲区的**只读标志一并带出**，紧接着的「把排除区清零」又往同一个数组里写，于是必然抛错。同一个函数里另两处 `np.asarray(Image...)` 不受影响（后面分别跟着 `> 0` 与 `astype()`/除法，都会产生新的可写数组）。修法是给这一个数组补 `.copy()`。
- **自 0.4.0 起就有，不是 0.4.1 引入的**：0.4.0 的同一函数、同一行赋值一字不差，`manual_exclusion_mask` 同样被传进 `predict_raw`、`return_foreground_probability=shape_refinement` 同样存在。拿冻结的 0.4.0 跑同一组输入，报的是同一个 `ValueError`。0.4.0 的用户没有遇到，只是因为当时没有同时开着形状细化并在图上画排除区。
- **修复后按真实流程重跑同一批图**：8 张全部成功（`failed: 0`），`0913-100-2 green.tif` 由 `FAILED` 变为 `worms=10 expected=10 PASS`。用 `run_gui_batch`（而非 `process_image`）复现，因为只有前者会从 checkpoint 读 `image_size`、腐蚀次数与各阈值。重跑写出的 `batch_summary.csv` 与用户那份原表逐字段对照：**除这一行之外 7 行完全相同**，包括 `0913-1k-2 green.tif` 原本就是 `REVIEW_COUNT_MISMATCH`（11 条）这一点。
- 单元测试数 131 → 133：`tests\test_manual_exclusion.py` 新增两项，**不打桩 `predict_raw`**、用一个返回常量 logits 的假模型驱动真函数——该文件原有的排除区测试一律把 `predict_raw` 打了桩，这正是这条 bug 一直没被发现的原因。两项在临时撤掉 `.copy()` 后确实失败，补回后通过。**这一轮只改了 Python，因此只重建 `AutoWormGUI.exe`，`Auto_Worm_ROI.jar` 未重建、与上一轮逐字节相同**（`A25142A5…AA2D5`）。逐字输出与哈希见 `VALIDATION_0.4.1.md`「真实数据实测发现的一处崩溃」一节。

## 0.4.0

- 新增 ImageJ 1.x/Fiji 插件入口和菜单。
- 将 Python 推理端改为 ROI-only ImageJ 后端，不再由 Python 计算荧光或 CTCF。
- 使用 ImageJ `Analyzer`/`ImageStatistics` 测量原始 TIFF，输出校准 CTCF 与 RawCTCF。
- 支持当前 TIFF 与当前文件夹批处理。
- 使用 ImageJ ROI Manager 标注头向、人工分界和背景排除区。
- 新增逐图 `.autoworm.json` sidecar，同时保留旧文件夹标注格式兼容。两种来源按标注类型合并：sidecar 中非空的一类覆盖文件夹记录，为空的一类沿用文件夹记录，因此只含头向箭头的 sidecar 不会丢掉同一张图在 GUI 里画的人工分界。在 GUI 中保存某张图时，若该图已有 sidecar，同一份结果会一并写回 sidecar，使 GUI 的删除和改动立即生效。已知限制见 README“手动标注”一节。
- 保留高清晰度/低清晰度模型、预设虫数、平滑修复和部分圈画。
- 部分圈画使用头到尾的单轨双滑块，启用时强制人工头向。
- 原 0.3.3 目录未修改。
- ImageJ 插件改为启动并复用 0.3.3 的完整原 GUI；每批 ROI 处理完成后 GUI 保持打开并自动把本批结果交回 ImageJ 测量，用户关闭 GUI 才结束插件。
- 修复 Fiji 1.54p 使用系统默认编码读取 `plugins.config` 导致的中文菜单乱码；菜单配置改为 ASCII，GUI 内继续支持中英文。
- CUDA 13.2、cuDNN、PyTorch 和 MSVC 运行库随软件发布；新增 NVIDIA GPU/驱动/CUDA 自动检查和系统信息窗口。
- CUDA 13.x 最低驱动系列设为 580；不要求目标电脑安装 CUDA Toolkit。
- 将人工头向与人工分界重构为两个平级算法：可分别独立使用；同时使用时先按头向粗分，再按人工分界精修。
- 人工分界现在可在虫数已正确但相邻 ROI 边界错误时重划像素归属，并保持前景总集合与实例数量不变。
- 人工分界精修只在真正改变像素归属时才写入 `*_manual_split_qc.csv`；分界已被当前划分满足时不再产生名不副实的 `manual_boundary_refine` 记录。
- 新增图像格式预检：只接受单通道、单层、单时间点的灰度 TIFF（8 位 / 16 位，含大端）。彩色 TIFF 和多页 TIFF 会在处理开始前被整批拒绝并列出文件名。之前这类图像不会报错，但程序只读取文件第一页、ImageJ 测量当前显示的平面，两者会静默地指向不同平面。多通道/堆栈数据请先用 `Image > Stacks > Stack to Images` 拆分。原因与处理方式见 README“支持的图像格式”。
- 预检的例外：多余平面全部为 0 的图放行。这类平面来自 MetaMorph 等采集软件附加的空 overlay 层，真正的数据仍在第一页，空平面不可能量出错误数值，因此没有拒绝的必要；放行时会在日志里写明该图有几个空平面。判定逐个检查第 2 页起的每一页，只要其中任意一页含非零像素就仍按多页拒绝。注意 ImageJ 不会跳过这些平面，它会把图显示成多个切片，测量前需确认停在的是第 1 个切片。
- 已知限制：金字塔 TIFF（全片扫描仪产物）可能把降采样层报成多个平面而被误判拒绝；被截断、损坏或无法解码的文件也一律拒绝；改名的非 TIFF（例如扩展名为 `.tif` 的 PNG）同样拒绝，因为 ImageJ 打不开它、否则会在测量阶段静默漏掉。此类文件都会点名列出，可转换或拆分后重试。
- 注意：预检会实际解码每张图一次，用于发现文件头正常但像素读不出来的半拷贝文件，因此开始前每张图多耗约 0.05 s。
- 注意：`补充样例2` 里 6 个 `Phsp16.2 0101-0-1 n=20.tif` 的第 2 页是 MetaMorph 写入的全零 overlay 平面（既不是 Z 层也不是通道），已按上面的例外放行，无需拆分即可处理；这 6 个文件在实测中均正常跑出结果。
- 单张图像处理失败不再终止整批：记下原因后跳过该图，其余图像照常处理并交给 ImageJ 测量。失败图在日志中记为 `Failed_<原文件名>: ERROR <原因>`，在 `batch_summary.csv` 中同样记为 `Failed_<原文件名>`（`qc_status` 为 `FAILED`，原因写入新增的末列 `error`），且不生成 ROI，因而在 ImageJ 侧被自动跳过。**磁盘上的原文件不会被改名**：`Failed_` 只是日志和汇总表里的记法。
- 测量完成后，若本批有失败图像，插件会弹出「部分图像未处理」并列出各失败图与原因，同时写入 ImageJ 日志。此前这类图像只是从测量表里消失，界面上没有任何说明。
- 修复失败图像仍会被 ImageJ 静默测量：程序在画 QC 图**之前**就已经写好了 `*_RoiSet.zip`，若该图随后失败，这个 zip 会留在输出文件夹里。而 ImageJ 只测量「存在 `<图名>_RoiSet.zip`」的图，于是同一批里一张刚被报为 `FAILED` 的图反而会被测进结果表，界面上却写着它失败了。现在失败时会把该图已写出的全部结果文件（`_RoiSet.zip`、`_measurements.csv`、`_QC.png` 及各类 QC 表）一并删除；极少数删不掉的（例如 QC 图正被看图软件打开）会在日志里写出 `Could not remove partial results for ...`，而不是默默留下。该处原有的注释把这件事写反了，一并修正。
- 原界面因异常中断时，插件现在显示该异常的原文，而不再一律显示「原界面处理失败，请查看 Auto Worm 日志。」。
- 批处理每处理完一张输出一行 `Progress: 3/12 (25%)`，失败的那张也计入进度，可在日志窗口判断剩余量。
- 新增结果重名预防：结果文件按「去掉扩展名的文件名」命名，因此同一文件夹下的 `a.tif` 与 `a.tiff` 会写到同一组文件名上并互相覆盖。检测到这种情况会在处理开始前拒绝整批并列出冲突文件名，不做静默覆盖。
- 二值（1 位）图像不再被拒：它们能用，只是两个模型都只在 8 位和 16 位采集图上训练过，因此开始前提示「本模型未针对二值图片进行训练」，确认后照常处理。
- 修复二值图像在批处理中途崩溃：此前 1 位图能通过预检、也带着「可以继续」的提示，但处理到它时会以 `TypeError: numpy boolean subtract, the '-' operator, is not supported` 中断该图。原因是 `np.asarray()` 对 PIL 的 `"1"` 模式返回的是布尔数组，而流水线的第一步就是背景相减。现在 `image_array()` 把它展宽成与 8 位图相同的 0/255，整条流水线（含标注视图）拿到的是普通 8 位数组。已用真实流水线端到端复现修复前后的差异。
- 预检弹窗一次列出的文件名有上限，超过时补一句「共 N 张不合格」，不再只显示前若干条而让人误以为只有这些。
- 合并 `manual_head_annotation.py` 中重复的 16 位字节序分支，改用既有的 `image_array()`，标注视图与分割引擎从此共用同一份像素读取逻辑；已用 65 个真实 TIFF 与 6 个合成文件逐一比对两种实现，结果完全相同，模型行为不受影响。
- 修正 `batch_summary.csv` 成功行的列错位：新增 `error` 列后，成功行没有补该列的占位，导致每行最后一个值（`software_version`）落到 `error` 列名下。现在表头、成功行与失败行共用同一份列定义，不再各自维护，并新增断言逐行核对字段数与表头一致。
- 补齐第三方许可声明：新增 `licenses\`，随运行包分发 `THIRD-PARTY-NOTICES.txt`（组件清单与许可全文位置）、NVIDIA CUDA Toolkit 最终用户许可协议、NVIDIA cuDNN 软件许可协议、Pillow 与 roifile 许可。此前运行包只含各 Python 包自带的零散许可文件，没有任何 NVIDIA 许可文本，也没有一份面向用户的组件清单。
- NVIDIA 许可文本取自官方文档站（`docs.nvidia.com`），未作删改，仅去掉网页导航与页脚，出处与 SHA256 记在 `THIRD-PARTY-NOTICES.txt` 里。据 CUDA Toolkit EULA 2.6 节 Attachment A，可再发行的 CUDA 文件允许在文件名中带版本号与架构信息，因此包内的 `cudart64_13.dll`、`nvrtc64_130_0.dll`、`cupti64_2026.1.1.dll` 等都在可再发行之列；cuDNN 不由该附件覆盖，依据是 cuDNN 软件许可协议补充条款第 2 节「可再发行的是运行库 .so 与 .dll」。
- Pillow 与 roifile 的许可全文此前并不在运行包内：PyInstaller 没有把这两个包的 `dist-info` 打进包里，而两者的许可都要求在二进制分发中附上全文，因此单独复制进 `licenses\`。
- `postbuild.py` 增加一步，把 `licenses\` 复制进 `dist\AutoWormImageJ\licenses\`；源目录缺失时报错退出而不是跳过，避免构建出一个没有许可声明的包。
- 文档补上使用限制：软件为科研工具，测量结果不得用于临床诊断或生命安全相关用途；并写明 NVIDIA 未对本软件作认可或担保，以及 EULA 2.2 / 2.3 / 2.5 对再分发的限制。依据 CUDA Toolkit EULA 1.1.2，随附声明 “This software contains source code provided by NVIDIA Corporation.”（该条原本针对示例源码的修改与衍生作品，本软件并未分发示例源码，属从宽保留）。
- 本软件自身的代码与文档改为 **MIT 许可**，全文见根目录 `LICENSE`，README 相应改写。此前 README 写的是「授权条款由项目作者另行确定」，对使用者等于没有授权。MIT 只覆盖本项目自己的代码与文档，随包分发的第三方组件（含 NVIDIA）仍按各自许可分发，不受影响；`licenses\` 的声明范围未变。
- 版本号明确标注为**测试版 beta**：`plugins.config` 的菜单项、`APP_VERSION`、`SOFTWARE_VERSION` 三处一致。README 开头新增「测试版说明」，写明已验证的范围，以及最希望得到反馈的是出错的情况。
- 打包结构改为可直接安装的形式：发布 zip 顶层是 ImageJ 惯例的 `plugins/`，解压到 Fiji 根目录（`ImageJ-win64.exe` 所在处）即可。此前 README 教用户从 `dist/` 里手工复制 `Auto_Worm_ROI.jar` 和整个 `AutoWormImageJ` 文件夹，路径前缀 `dist/` 对拿到发布包的最终用户并不存在。
- 新增 `make_release.py` 打包脚本，在打包前做三项检查，每一项都对应一种「打出来的包看起来完全正常」的错误：版本号必须与 `plugins.config` 一致（`plugins.config` 是唯一事实来源，脚本自己不再复述版本号）；EXE 内每个模块的代码对象必须与当前 `src\*.py` 相同，否则拒绝打包；给了 `--previous-exe` 时还要求**旧包必须与当前源码不同**，否则「新旧一致」就成了一句空话。比对方式不依赖任何需要人工记得添加的标记字符串（指纹的构成后来加强了，见本文件末尾）。已用重建前的旧 EXE 证伪过：旧包 10 个模块相同、恰好 3 个不同（就是本次修改的 `batch_worm_roi`、`inspect_roi_dataset`、`worm_roi_gui`），新包 13 个全部相同。该脚本还会把四份文档刷新到 `dist/`，手工同步这一步从此取消。
- 新增 GUI「保存日志」按钮（终端标题栏右侧，右键菜单同项）：弹另存为对话框让用户选路径，默认文件名带时间戳。测试版需要用户把日志连同问题一起发回，而日志窗口的内容在关闭 GUI 后就没有了。
- 调整终端面板标题栏：将“保存日志”按钮收进全宽标题栏，使终端正文、滚动条、标题栏和监视器面板右缘统一对齐；隐藏 GUI 结构检查和实际渲染检查均已通过。
- 新增 `diagnostics/make_test_samples.py`：生成覆盖各类不合格图像的合成 TIFF 测试样例，覆盖 10 种情形（正常、含信号的多页栈、彩色、截断/损坏/非 TIFF、空 overlay 平面、二值、同名不同扩展名、混合目录，以及一批「预检放行但处理中途失败」的畸形尺寸图）。生成后用程序自己的 `check_image()` 逐张复核，判定与预期不符即报错退出。本机没有这些异常图像的真实样本，报错相关的功能因此一直没有数据；上面两个 bug 都是把真实流水线跑过这些样例才暴露出来的。
- 修复测试套件实际测的是别的工作区的代码：`tests\test_erosion_0101.py` 名字匹配 `test_*`，会被 `unittest discover` 导入，而它在模块级就把**旧工作区**（`备份\0910 0.3.2 CUDA\src`）插到 `sys.path` 最前面并 `import inspect_roi_dataset`，于是 `sys.modules` 里留下的是 0.3.2 的那一份。名字一旦绑定，后面再往 `sys.path` 前面插当前 `src` 也不起作用，因此整个测试进程里 `inspect_roi_dataset`（以及经它间接使用的 `batch_worm_roi`）跑的都是 0.3.2 的代码。**此前没有任何一条测试失败**，这正是问题所在：测试看起来全绿，测的却不是本仓库。改名为 `erosion_0101_probe.py` 后 discovery 不再导入它；`test_shape_refiner_032.py` 同样只注册 0 个用例，一并改名为 `shape_refiner_032_probe.py`。两个文件都保留原样并加了说明注释，不是删除。
- 新增套件完整性测试 `SuiteIntegrityTests`：断言 `batch_worm_roi`、`inspect_roi_dataset`、`worm_roi_gui` 三个模块确实从本仓库的 `src\` 导入。上面那个问题正是被新加的 1 位图测试撞出来的——它在完整套件里失败、单独跑却通过，才暴露出模块来源不对。
- 单元测试数 78 → 79（新增套件完整性测试 1 项；两个被改名的脚本本来就不注册用例，数量不变）。
- 新增 Java 桥接清单的单元测试：Base64/UTF-8 往返（中文与多行消息）、不残留 `.tmp`、未设清单变量时不写任何文件、路径不可写时报告而不是抛出、失败清单的 10 条上限与「共 N 张」计数。这段代码跨进程边界，此前只靠人工界面验收。
- README 重写为 Markdown，重心从「发布准备」移到**功能介绍与使用帮助**：新增界面参数一览表、逐项功能说明（模型选择、预设虫数、平滑修复、手动标注的合并规则、部分圈画）、**结果文件说明表**（每份产物由谁写出、何时写出，此前从未集中交代过）和「常见问题」。
- 随包 README 定稿为面向 Java 8 用户的版本：文件名 `README_0.4.0_ImageJ_Java8.md`（此前为 `README_0.4.0_ImageJ.md`，最初的 `.txt` 已删除），标题标明 ImageJ / Fiji (Java 8)。相对上一稿补充了反馈渠道（两个邮箱与 GitHub issue）、模型训练所用显卡（RTX 4060 Laptop，未在其他型号上验证）、`RawCTCF` 属于已废弃算法但仍保留的说明，以及安装一节的硬件与软件要求。`make_release.py` 的 `DOC_NAMES` 随之改为 `README_%s_ImageJ_Java8.md`。
- README 为后续正式版发布做了延续性处理：正文不再散写版本号和 beta 字样，测试版专属内容集中到「测试版说明」一节，菜单项写法改为「名称形如 `Auto Worm ROI <版本号>`」。正式版发布时只需改开头那一行版本/状态、删掉该节，其余文字原样可用。相应地 `make_release.py` 的 `DOC_NAMES`、模块注释里的文档清单和 `AGENT.md` 第 7 节都改为 `.md`。
- 修正 README 中关于监视器边框的一处错误说法。原文在「平滑修复」一节写「调用该功能后，监视器上的对应图像边框会标黄」，这与代码不符：黄框的触发条件是该图 `batch_summary.csv` 里 `shape_refined_count`、`shape_review_count`、`low_clarity_split_count` 任一大于 0——即**这张图有虫体被改写过或被标记**，其中只有前两项来自平滑修复，第三项来自低清晰度模式的粘连拆分；开了平滑修复但一条虫都没改动、也没被标记的图不会标黄。该句已删掉，改为新增「监视器上的边框颜色」一节，逐条写明红框（`qc_status` 以 `REVIEW` 开头，实际只有 `REVIEW_COUNT_MISMATCH`，即识别数量与预设虫数 n 不等，界面上该图左上角另加一行「需人工复核 ｜ <原图名>」）、黄框、蓝框（当前选中，与结果好坏无关）的触发条件与「红 > 黄 > 蓝」的优先级，说明红黄两色是**写进 `*_QC.png` 文件本身**的（用别的看图软件打开、发给别人都在），并写明手动标注与部分圈画不会单独触发黄框、失败图不会出现在监视器里。另修正 5 处 `NVDIA` → `NVIDIA` 拼写。随包 zip 重新打包，`jar` 与 `exe` 未变。
- ImageJ 状态栏的 6 处提示改为英文（`plugin_src\Auto_Worm_ROI.java` 的 `IJ.showStatus`）。此前菜单是 ASCII 英文（Fiji 1.54p 读取 `plugins.config` 的编码问题，见上），状态栏却全是中文，两处语言不一致。现在依次为：启动后 `The GUI has been opened. Please perform image processing in it.`、测量完成 `Measurement complete. The GUI stays open; you can process another folder.`、本次取消 `Processing was cancelled. The GUI stays open.`、未测量就关闭界面 `The GUI was closed without any ImageJ measurement.`、关闭后汇总 `The GUI was closed. ImageJ measurement batches completed: N`、测量结束 `ImageJ measurement complete. Images measured: N`。两条带数字的写成 `...: N` 而不是 `N batches` / `N images`，这样 N 为 1 时不需要复数变形。`IJ.log` 日志与 `IJ.error` 弹窗（含「部分图像未处理」「ImageJ 测量失败」）**仍是中文**：README 与 VALIDATION 引用的是这些弹窗文字，因此两处文档正文不需要改。插件 jar 已用 `javac --release 8`（JDK 21，与 `build_0.4.1.bat` 第 1 步相同）重新编译：与上一版 jar 逐项比对，**只有 `Auto_Worm_ROI.class` 不同**；再用与常量池下标无关的方式解析两份 class，确认差异只有上述 6 处字符串字面量，代码引用的类、方法、字段、描述符与数值常量全部相同。此外把未改动过的源码（`git show HEAD:` 的版本）重新编译一次，产物与发布包里的 class **逐字节相同**，证明本机 javac 可复现，「只差字符串」这一结论因此成立。`AutoWormGUI.exe` 未重建（本轮没有改 Python 代码），EXE 哈希不变，`make_release.py` 的「包内代码与 `src\` 一致」检查照常通过。
- 修复 ImageJ 测量可能把**另一张图像**的像素算进结果表：测量此前走 `ij.plugin.filter.Analyzer.measure()`，而它读的是 `Analyze > Set Measurements` 里的 `Redirect to` 那张全局静态设置。用户一旦勾过 `Redirect to`，所有测量都会改从被指定的那张图取值，而结果行的 `Image` 列仍写着原本要测的图名；更糟的是同一行里的 `RawIntDen` 来自 `getRawStatistics()`，它**不受**重定向影响，于是同一行会同时出现来自两张图的数字，界面上没有任何提示。现在改为直接调用 `image.getStatistics(...)` 取全部统计量，不再经过那个全局开关。已实测四种 ROI（Roi / OvalRoi / PolygonRoi / ShapeRoi）× 标定与未标定两种情况：新旧两条路径的 Area / Mean / Min / Max / Median / IntDen 完全一致（未标定例 `Area=2700.0000 Mean=2021.337037 Min=1024.0 Max=4095.0 Med=1986.0 IntDen=5457610.0000`，标定后 `Area=337.5000 IntDen=682201.2500`），装上重定向后 `Analyzer` 的 Mean 变成 `2039.955556` 而 `getStatistics` 仍是 `2021.337037`。数值语义因此没有变化，只是不再受本机全局设置影响。同时删掉了随 `Analyzer` 一起写出来、用于兜底的 `value(...)` 取列助手——它会在列缺失时静默回退到原始统计量，正是同一行混入两张图数字的第二个来源。
- 修复 ImageJ 测量的可能是**窗口里改过的图**而不是磁盘文件：ROI 是另一个进程从磁盘文件画出来的，若用户在 ImageJ 里对这张图做了裁剪、反色、滤波等改动而尚未保存，旧代码会直接把窗口中的图拿来测量（`sameFile(input, activeFile) ? active : IJ.openImage(...)`），量到的像素与 ROI 已经对不上。现在一律重新从磁盘打开该图测量、测完立即关闭，窗口内容不参与。此外在开始前检查当前图有无未保存改动（`ImagePlus.changes`），有则弹窗说明「处理的是磁盘上的版本，屏幕上的改动不会参与」，点「确定」继续即表示接受。该标志位已实测确认可用：刚打开的图为 `false`，`IJ.run Invert` 与 `Subtract Background...` 之后为 `true`，保存后回到 `false`；而直接写像素的 `putPixel` 不会置位——它反映的正是 ImageJ 命令层的改动，也是唯一需要提示的那一类。测量阶段还保留一条日志：确实测到当前图且它仍有未保存改动时，写明本次测的是磁盘上的哪个文件。
- 修复批处理通知会被覆盖、导致整批结果无人测量：桥接协议此前约定**一个固定路径**的清单文件，GUI 每完成一批就覆盖写它，ImageJ 读完即删。当 ImageJ 还在测量上一批（测量可能要几分钟）而 GUI 又完成了一批时，新清单会盖掉尚未读取的旧清单，那一批的结果就永远没有接收方；「读完再删」本身也存在删掉刚写进来那份的时间窗口。现在改为**通知队列**：环境变量换成目录（`AUTOWORM_IMAGEJ_BRIDGE_DIR`），每批写一个独立文件，名字是 20 位零填充序号（`00000000000000000001.properties`，零填充正是为了让「按文件名排序」等于「按写入顺序」），写入仍是先写 `.tmp` 再 `os.replace`；Java 侧按名字顺序遍历、**只删除自己刚读到的那一个文件名**，读完再判断进程是否存活，退出时清空目录。**（这两处时机在本文件末尾的复审修复里又各改了一次：改为「处理完才删」并「退出后把队列读空」，原因见那两条 P1。）**协议因此不再需要「关窗前检查有没有未读清单」这类保护：关闭通知是队列里的一个新名字，一定排在等待中的批次之后被读到，`_on_close` 里原来的那个 `is_file()` 判断随之删除。跨版本配对（新的 `AutoWormGUI.exe` 配旧的 `Auto_Worm_ROI.jar`）会因此静默失效，所以系统信息窗口在「ImageJ 插件桥接」模式下增加一条警告：未收到桥接目录时提示两个文件必须来自同一个发布包——这一组合只可能由版本不匹配造成，否则用户既看不到结果也看不到原因。
- ImageJ 侧改为按 GUI 报来的**完成清单**测量，而不是按「存在 ROI ZIP」猜测：`*_RoiSet.zip` 是在一张图处理完**之前**就写好的，若它随后失败且部分结果删不掉（例如 QC 图正被看图软件打开），这个 zip 会留在输出目录里，于是同一批里一张刚被报为 `FAILED` 的图反而会被测进结果表。现在 GUI 在完成通知里带上 `ok_count` 与实际文件名（`ok_b64`，按名排序、去重），插件只测这些；名字按文件名的原样比较，不做大小写折叠、不比较去扩展名的短名——两边列的是同一个文件夹，不需要任何归一化，也就不会把两张图对错。清单缺失（旧版 GUI 或 `--headless-run` 诊断路径）时退回原有规则；清单与计数不符时以清单为准并在日志里写明。
- 加强 `make_release.py` 的发布包校验指纹：原指纹只取 `co_name` / `co_names` / `co_varnames` 与字符串常量，于是**改一个数字它看不见**——`x + 1` 改成 `x - 99`、`LIMIT = 580` 改成 `579`，指纹都与原版完全相同，校验会放行一个用旧源码构建的 EXE，而这正是该检查存在的理由。现在指纹补齐：整数/浮点/bytes/None/元组等全部常量、`co_argcount` / `co_posonlyargcount` / `co_kwonlyargcount` / `co_flags`、自由变量与闭包变量、以及**字节码本身**（`co_code`）。行号与 `co_firstlineno` 有意不含：它们能抓到的只有空白与注释，改不动程序行为，加进去只会让检查对格式敏感。改完后用四项实验证明：`x + 1`→`x - 99` 旧指纹判为相同、新指纹判为不同；`580`→`579` 同样如此；22 个 `src\*.py` 编译后经 `marshal` 往返（即 EXE 里存放的形态）指纹全部一致；代码反复执行 20 万次触发 3.13 的字节码特化后指纹仍不变。最后拿现存 EXE 实跑：12 个未改动模块全部判为相同、只有本次改动的 `worm_roi_gui` 判为不同，与预期逐一吻合。
- 顺带修掉加强指纹时暴露的一个**假阳性**：`repr()` 打印 `frozenset` 的顺序是它内部散列表的顺序，而这张表在编译时按源码顺序填充、从 `.pyc` 读回时按存储顺序填充——同一个未改动的 `frozenset` 真的会打印出两种顺序，`evaluate_shape_refiner.py` 里就有一个四项的，加进指纹后它被判为「与源码不同」。指纹现在先把集合元素各自渲染成字符串再排序，因此与顺序无关；元组是有序的，照旧按顺序比较，两者都写进了单元测试。
- 随包 README 新增「当前图像有未保存的修改时」一节（写在「快速上手」之后，由用户指定位置与内容）：说明调用插件时会先弹窗确认、处理的是磁盘上保存过的版本、点「确定」与「取消」分别意味着什么，以及「要把改动一起处理就先保存再重新调用」。这一段是上面「测量一律重新从磁盘打开」那条修复的**用户可见后果**，不写出来用户只会看到数值与屏幕上不一样而没有解释。
- 单元测试数 86 → 98：新增 `tests\test_make_release.py`（12 项，覆盖改动数字/字符串/查表名/嵌套函数/参数表/默认值必须判为不同，注释与空行不得判为不同，`src\*.py` 全部经 `marshal` 往返必须判为相同，以及无序容器与嵌套容器两种渲染），并扩充桥接通知测试（第二批不覆盖第一批、`.tmp` 不被当作通知、`ok_count`/`ok_b64` 往返、空清单仍是清单、无清单时不写、`_successful_images` 排除失败图）。
- **修复关窗时丢掉队列里尚未测量的批次**（复审 P1）：轮询循环每轮只在开头取一次队列快照，而测量是在快照的循环体里跑的。ImageJ 测 A 的几分钟里 GUI 完成 B 并把关窗通知排在 B 之后，A 测完时快照已耗尽，`if (!process.isAlive()) break;` 因进程已退出而跳出，紧接着的 `removeBridge` 把 B 连同目录一起删掉——用户在 ImageJ 里看到的是这一批只有 ROI、没有任何测量结果。现在进程退出后仍会把队列读空：循环条件是「进程已退出**且**本轮没有任何进展」，进展由 `progressed` 标志（本轮是否删掉过通知）记录，它同时保证循环必然结束。用 `tests\AutoWormBridgeQueueTest.java` 复现过：把这两处行为临时改回旧写法后该测试在第 75 行失败（`batches measured: expected 2, got 1`），改回来后通过。与审查报告给出的现象（`A_MEASURED=true`、`B_MEASURED=false`）一致。
- **修复「等待 ImageJ」闸门放行过早**（复审 P1）：Java 侧原来在**读取**通知时就把它删掉，而测量在之后才开始。GUI 用「桥接目录里还有没有 `.properties`」判断 ImageJ 是否已处理完上一批，于是读一次（毫秒级）就把闸门打开，而测量要跑几分钟：用户在这段时间开始下一批、又选了同一个输出目录，第二批写的 `<stem>_RoiSet.zip` 会顶掉 ImageJ 还没读到的 ROI（它是逐图读 ROI ZIP 的），两批结果混在一张表里、combined 表也互相覆盖。现在**处理完一个才删一个**（读失败的文件仍然立即丢弃，否则会每 150 ms 重读一次），于是「目录里没有 `.properties`」才真正等于「都已测完」。GUI 那句提示与 `_imagej_bridge_pending()` 的注释同步改成「正在测量上一批，测量完成后才能开始下一批」（中英双语，英文为 `ImageJ is still measuring the previous batch. Please wait for it to finish before starting the next.`）。同样由上面那个 Java 测试钉住：改回「读到就删」后它在第 110 行失败（`in the queue while being measured: expected [true], got [false]`）。
- 轮询循环从 `execute()` 里提取为 `pumpNotifications(Path, Process, BatchHandler)`，返回已测量批次数；`Handled`（MEASURED / UNMEASURED / CLOSED / OTHER）与 `BatchHandler` 与它同包可见——这样 `tests\AutoWormBridgeQueueTest.java` 能直接驱动**已构建的 jar**，而不是重新编译一份源码。测试用 `--sleep` 另起一个进程冒充 GUI（「进程何时消失」必须是真事），四种情况全部覆盖：退出后补测、测量期间通知仍在队列、删不掉的通知只测一次（用 `dos:readonly` 造出真的删不掉，比用打开的文件更可靠——JDK 是带共享删除打开的）、读不了的通知被丢弃且不会让循环空转。三次运行都用 Fiji 自带的 Java 8 运行时（与用户实际运行环境一致）。
- **修复发布校验指纹丢失顺序**（复审 P2）：`code_surface` 把每行指纹塞进 `set`，顺序与归属一起丢掉。`a = 1; b = 2` 与 `a = 2; b = 1` 的**字节码逐字节相同**（`950053007200530172016702`，变的是 `co_consts` 由 `(1, 2, None)` 变成 `(2, 1, None)`，取常量的指令没变），于是两者被判为相同；`{"x": 1, "y": 2}` 换值、两个函数体互换、嵌套函数与常量交换位置同理全部漏判。现在改为**有序列表**：按编译器给出的顺序记录常量与各名字表，嵌套代码对象在它在 `co_consts` 中的位置展开（块的头部是它自己的 `code <名字>(...)`，因此归属无歧义）。实测：上面四类改动全部判为不同；`src\*.py` 全部 22 个模块经 `marshal` 往返仍判为相同（**没有引入误报**，包括带 `frozenset` 的 `evaluate_shape_refiner.py`，`constant_key` 继续负责单个无序容器的规范化）；用**上一轮构建的 EXE**（未含本轮改动）比对当前源码，恰好只有 `worm_roi_gui` 报 `differ`、其余 12 个模块报相同，说明有序化没有把未改动的模块判成不同。
- 单元测试数 98 → 103：`tests\test_make_release.py` 12 → 17（新增交换模块常量、交换函数体、嵌套函数与常量换位、字典换值四项必须判为不同；以及「换序判据经 `marshal` 往返后仍然成立」），`tests\test_imagej_integration.py` 中那条闸门测试的注释改为说明「已处理」而非「已读」。本轮**同时改了 Python 与 Java**，因此 jar 与 EXE 都必须重建（Python 源码改动即使只是注释与提示语，`make_release.py` 的指纹比对也会要求重打包）。
- **修复背景 ROI 可能压在虫体或人工排除区上**（复审 P1）：背景矩形的位置由「虫体左边界减去图像宽度的 2.5%」算出，且宽度被强制为至少 5 像素，因此虫体一旦贴近左边缘，矩形的右边缘就会落进虫体；人工排除区盖住默认位置时，后退逻辑也只在排除区呈整块矩形时生效。结果是「统计的背景」与「导出给 ImageJ 的背景 ROI」变成两块不同的地方：Python 后端报的是挖掉重叠部分后的均值，ImageJ 后端量的是整块矩形，同一张图在两种后端下得到不同的 CTCF，而 QC 列两次都是 `PASS`。实测（400×400 合成图）改前背景 mask 为 0 像素、导出的 ROI 却覆盖 660 个虫体像素，或覆盖 19910 个已排除像素。现在改由「候选矩形必须是完全干净的区域」来保证两者一致：依次尝试虫体左侧、右侧、上方、下方，取可行面积最大的一块，矩形内不允许出现任何虫体像素或排除像素，因此 `mask` 与导出的矩形天然是同一块地方。一处都找不到、或最大者小于 200 像素且不足整图面积的 0.1%（避免 110 像素那种细长条）时，该图按单图失败处理并在原因里写明「找不到干净背景区域」——与其报一个被污染的均值，不如让用户去看排除区画得对不对。
- **修复人工排除区在虫体内部时被填回**（复审 P1）：虫体 ROI 此前一律导出为一圈普通多边形，而普通多边形没有「这块不算」的表达方式。人工排除区若整个落在虫体内部（例如排除掉虫体上的一块脏污），圈虫算面积与均值时把它挖掉了，ImageJ 却按外轮廓把它填回来重新计入。实测同一只虫改前 Python 侧面积 13250/均值 100.0000、导出的 ROI 面积 14300/均值 122.0280，1050 个被排除像素全部重新进入统计。现在这类 ROI 导出为 ImageJ 的复合 ROI（`ShapeRoi`）：外轮廓子路径加一条**绕行方向相反**的孔洞子路径，非零填充规则据此把洞挖掉（没有任何标记位，靠的就是方向）。没有孔洞的虫体仍导出为原来的普通多边形，行为不变。格式是拿 ImageJ 自己写出的带孔 ROI 逐字节读回来的，不是猜的：三元组 `(操作, x, y)` 平铺，0=起点、1=连线、4=闭合子路径，坐标为绝对坐标，文件头仍是矩形类型（`roitype=1`）外加 `shape_roi_size`，版本号 228、大端。端到端实测：ImageJ 加载导出的 ROI 得到 `ShapeRoi`，被排除的 630 像素不再进入统计，虫体均值与 Python 侧同为 100.0000。
- **修复 GUI 退出瞬间写入的通知被丢弃**（复审 P2）：轮询循环在每轮**扫描完队列之后**才询问进程是否存活。GUI 是「写最后一个通知」与「退出」当一步做的，若这一步正好落在扫描与存活判断之间，这一轮什么也没扫到、紧接着看到进程已退出，于是「进程已退出且本轮无进展」成立、循环结束——那一批 ROI 已在磁盘上，却没有任何测量结果。窗口只有毫秒级，但它是每次使用的正常收尾，丢的正是最后一批。现在存活状态在**每轮扫描之前**采样，于是「已退出」这个判断至少落后一轮：采样到进程还活着的那一轮，扫描一定发生在它写完所有通知之后；采样到进程已退出时，这一轮仍会再扫一次，确认无进展才结束。
- **修复整批失败后上一批的合并测量表仍在原地**（复审 P2）：`ImageJ_measurements_all.csv` 只在测到内容之后才写，因此一批若一张都没测成（全部图像处理失败、用户在测完前关窗），输出文件夹里留着的就是上一批的数值，而文件里没有任何字段说明它来自哪一批，事后无法分辨。现在每批开始测量前先删除这张表，删前核对首行是否含 `CTCF` 与 `MeasurementBackend`（按列名判断而不是整行比对，旧版本写的列顺序不同也认得出来）；首行不是我们的表就不动它——输出文件夹是用户的，文件名相同不能作为「这是我们的文件」的证据，误删用户自己的同名文件比留一张旧表更糟。
- **修复测量阶段单张失败中断整批**（复审 P2）：逐图测量循环体原先只有 `finally` 没有 `catch`，`loadRois` 在 ROI ZIP 截断或读不出时抛出的异常会直接穿出整个测量过程，后面所有图像都不再测量；而合并表是在循环之后才写的，于是这一轮**已经测好的那些图也一起丢掉**。实测改前：第一张的 ROI ZIP 损坏，异常在 `measureOutputFolder` 处逃逸，第二张（完好的）从未被测量、合并表也没有生成。现在每张图单独捕获异常，记日志并计入跳过数后继续；全部图像都没测成时仍然报失败（消息里带上跳过张数），完成日志与状态栏也会写明跳过了几张。
- 单元测试数 103 → 110：`tests\test_manual_exclusion.py` 4 → 11（新增：背景 ROI 不得覆盖虫体或排除区且必须与统计区域逐像素一致、排除区盖住全部候选位置时不报背景、干净区域过小时不报背景、虫体内部的排除区必须导出成孔洞且两条子路径反向、无孔洞时仍是普通多边形、以及「没有干净背景时经 `process_image` 报错并在消息里点名该图」）。Java 侧 `tests\AutoWormBridgeQueueTest.java` 4 → 5 项（新增「退出瞬间写入的通知仍被测量」，用一个在 `isAlive()` 被询问的那一刻写出通知的 `Process` 精确复现那一毫秒的时序，旧 jar 上该用例报 `batches measured: expected 1, got 0`），并新增 `tests\AutoWormMeasurementTest.java`（3 项：坏 ROI ZIP 之后的图像仍被测量并写进合并表、上一批的合并表在测量前被删掉、同名的非我方文件不被删除也不被覆盖）。
- 本次重建的产物已在真实数据上验证（不只是单元测试）：三张真实样例以无界面模式跑批全部 `PASS`，导出的 ROI 逐像素复核后确认背景矩形与虫体**零重叠**；另在一张真实图上按界面写标注的格式给某条虫内部加了一块人工排除区，重新跑批后该虫导出为复合 ROI，ImageJ 读取后确实把洞挖掉（带洞 44253 像素 / 去掉洞 44325 像素，差的正是那块排除区）。同一只虫在两种后端下的面积当时仍相差约一圈边界像素（约 2%）——那是 0.4.0 一直存在的取样约定差异，已在 0.4.1 修复，见本文件开头与 `VALIDATION_0.4.1.md`。

### 第四轮：人工分界线的画法与严格遵循（2026-09-15）

- **分界线改成逐个节点取点**：在界面里画人工分界线，现在是**左击落一个节点、移动鼠标拉出橡皮筋、右击结束**（与 ImageJ 的 Polygon Selections 一致），按 Esc 放弃正在画的这一条。此前是按住左键拖出自由曲线、松开即结束。已落的节点之间显示为虚线折线并带节点标记，与已经保存下来的青色实线区分开。**折线的拐点不再抽稀**：节点是实验员点出来的，抽稀会让线跑偏；只有双击落在同一处（相距不足 1 像素）的重复点会被去掉。排除区域仍是按住左键画一圈、松开闭合，未改动。
- **保存下来的分界线按折线显示，不再画成样条曲线**：算法里的那堵墙就是按折线画的，显示成曲线会让屏幕上看到的线与实际生效的线对不上。
- **人工分界线现在是硬约束**：以往那堵「墙」在生长算法里只是**代价高**，不是走不过去，生长前沿会从线的**自由端绕过去**，最后接缝落在两边前沿平衡的位置——在带标注的真实样例（18 条分界线、10 条虫）上量过：沿每条线取 220 个采样点，从采样点沿法向往两侧找第一个不同标签的像素，**约 13% 的采样点偏差在 5 像素以上，最坏 18 像素**（中位距离倒是只有 1 像素，坏的是局部几段）。现在生长结束后会按每条分界线把接缝**压回线上**：以最近的「芯」（生长起点）为准判断这条线哪一侧该归哪条虫，只在分界线周围一条窄带里改动，且只交换「本来属于对面那一侧」的像素，第三只虫的像素一个都不动；改动后每个标签必须仍然完整（非空且连通）才被接受，否则这一条分界线维持原样、其余分界线照常处理。同一张图的同一套采样由 **87.3% 提升到 93.2%**（0.4.1 原有的软惩罚实现是 91.8%），中位距离仍是 1 像素，虫数与 `manual_boundary_refine` 报告数都不变。剩下的偏差集中在几条线的端部，那里「线的自由端」与虫体实际缝隙的位置本来就不一致。
- **接缝偏离超过一定距离时不硬压**：压线只在分界线周围一条按图像尺寸算出的窄带（实测取图像短边的 4%，1024 像素的图约 40 像素）内进行。偏离比这还远时，强行压过去会从带外切出一条残条，此时该条线保持不动——宁可维持现状，也不产生一个不属于任何虫体的碎块。这个行为有单元测试钉住。
- **两条走不通的路（记录下来，避免重走）**：把墙做成完全不可穿没用（实测接缝到线的距离只从 1.90 像素变成 1.87 像素，说明前沿本来就没穿墙，而是绕过去的）；把墙延长到图像边缘则明显变差（87.3% 降到 68.6%，精修报告由 2 条降到 1 条，因为延长后的墙会把别的虫体切开）。
- **随包 README 同步更新**（经用户确认后修改）：「手动标注」一节新增三种标注在界面里的具体用法（头向箭头怎么拖、人工分界怎么左击落点与右击结束、排除区怎么画一圈），并写明人工分界线是**硬约束**及其生效范围——压线只在分界线周围约图像短边 4% 的窄带内进行，实际缝隙离画的线更远时维持模型划分，以免切出不属于任何虫体的碎块。另外清掉了验证记录里三处会随包发布的开发机绝对路径（探针所在目录、构建用 Python 路径、用户数据盘路径），改为描述性说法，**事实与数值未变**。
- 单元测试数 133 → 136：新增 3 项针对压线规则的合成用例（接缝压到线上、偏离超出窄带时不硬压也不切碎虫体、线两侧本来就是同一只虫时一个像素都不改）。界面隐藏自检新增一段多边形取点流程的断言：落点累积、分界线模式下松开左键不结束、右击结束并存入、两个节点且过短时拒收、Esc 清空草稿、撤销能退回去。
- **本轮只改了 Python，因此只重建 `AutoWormGUI.exe`**，插件 jar 未重建（Java 源码未改动）。

### 第五轮：三条并排的虫画了箭头也拆不开（2026-09-15，用户实测报告）

用户报的原话：这张图上「从左往右第 2、3、4 条虫子判成一条，**即使在我已经进行了人工分割的情况下也是如此**」。

- **现象**：`0913-0-2 green.tif`（1024×1024、10 条虫）上有 10 支头向箭头、4 条人工分界线、1 个排除区。模型把第 2、3、4 条并排的虫输出成**一个** 95104 像素的连通块（另外 7 块各约 3.2~3.7 万），结果该图只数出 **8** 条虫；用户画的三支箭头与四条分界线**一点作用都没起**，报告里 `人工拆分=0`。
- **根因一（箭头没起作用）**：`_partition_safety` 要求**每支箭头各自投票到一个不同的子块**。三支箭头都画在虫头一端，而虫头是三条虫挨得最近的地方（实测相邻虫头只隔 40 像素），箭头又短，于是两支的杆子落在了同一条虫那侧。实测这一次的候选分区本身是教科书级的：三块面积 32133 / 31180 / 31791，纵向跨度比 0.971 / 0.988 / 1.000，**每块都是单连通、前景一个像素不剩地分完**——可尾部投票是 **[1, 1, 3]**，两支投到了同一块，整份分区因此被否。另两条候选路径也都没拿住：按纵轴划核心返回 `None`，按纵轴归属的兜底方案退化成 [4030, 9820, 81254]（等于没分）。三条路径全被拒，粗分阶段就静默不动了。
- **根因二（分界线跟着失效，是根因一的连锁）**：粗分没做，那个 95104 的块还在，于是四条分界线被合并成**一组**、标签集是 {1,2,3,4}；这一组走的是「每个标签一个核心」的精修路径，而其中标签 2 自己就是三条虫，长出来的归属退化成 [69968, 36035, 94792, 32791, 34318]，指标算不出来，于是**用户画的分界线也一并没了作用**。
- **修法一（`src\manual_head_annotation.py`）**：投票认不出归属时，退到**按次序配对**——「实验员从左往右一只虫画一支箭头」这件事仍然成立，所以把箭头与子块各自沿**虫体横排方向**（与共同头向垂直的方向）排序后一一对应。为了避免「随便什么分区都能配上」，配对结果还要过一道闸：**至少一半箭头自己的投票与配对结果吻合**才接受，全都对不上说明这份分区与箭头无关，仍然拒掉。新增的 `_ordered_arrow_assignment` 同时被粗分与安全检查复用（`_across_axis` 从两处重复的「求横排方向」里抽出来）。
- **修法二（报告里的头部信息）**：拆开之后 `03_worm` 一行仍是 `MISSING`。原因是第 3 支箭头**从头到尾整根都落在第 2 条的像素上**（尾端 (385, 207)、头端 (407, 164) 都是标签 2），按「最近的箭头先配对」的贪心规则，它永远抢不到第 3 条。现在 `match_head_directions` 在贪心配对之后做一次同样的次序补救：**落空的箭头数恰好等于没人认领的虫数**时按横排次序配对，`match_method` 记为 `ordered`，并把箭头尾端到该虫的真实距离写进 `match_distance_px`（本次 10.9 像素）。多一支或少一支都不猜——画在背景上的箭头、以及「两支箭头压在同一条已配好的虫上」的情形一律不动，该报 `MISSING` 还报 `MISSING`（有单测钉住）。
- **实测（同一张图，同一套标注）**：

  | | 修复前 | 修复后 |
  | --- | --- | --- |
  | 虫数 | 8 | **10**（该图预期 10） |
  | `manual_head_split_count` | 0 | **1**（`2 -> 2\|3\|4`） |
  | `head_annotation_count` | 9（`03_worm MISSING`） | **10**（`03_worm` 为 `ordered`） |
  | 四条分界线贴合度（沿 4 条线取 3744 个采样点，接缝落在 5 像素内的比例） | 1913（51.1%） | **3037（81.1%）** |

  最后一行说明分界线**也跟着恢复了作用**，它量的正是交付产物：把输出目录里的 `0913-0-2 green_RoiSet.zip` 读回来重建标签图，再沿分界线采样，而不是拿中途某个数组充数。
- **回归面**：同批另外 7 张真实图逐字段对照，**一行都没变**（含 `0913-1k-2 green.tif` 原本就是 11 条 `REVIEW_COUNT_MISMATCH` 这一点）。单元测试 **136 → 140**：`_partition_safety` 的次序配对两项（三支箭头压在同一条上仍能拆开；三支全落进同一块时**不许**拆），`match_head_directions` 的次序补救两项（压到邻虫的箭头按次序配上、且只有「落空数 == 无人认领数」时才补；否则仍报 `MISSING`）。新用例在**关掉回退**的实现上确实是红的（三支箭头那组只剩 1 条虫；报告那组第 3 条为 `MISSING`）。
- **本轮只改了 Python**，因此只重建 `AutoWormGUI.exe`。jar 虽然被顺带重编译过，但逐条目比对确认 12 个条目**内容逐字节相同**（只差 zip 时间戳），因此**按上一版原样放回**，交付的 jar 与 0.4.1 此前各版一致。
