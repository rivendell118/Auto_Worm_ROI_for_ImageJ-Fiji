# Auto Worm ROI 接入多模态大模型 API 的可行性论证

**论证对象：** Auto Worm ROI 0.4.2 CUDA for ImageJ（Java 8）
**候选服务示例：** DeepSeek-V4.1-Flash（API 模型名：`deepseek-flash`）
**初稿日期：** 2026-09-15（针对 0.4.1）
**本版日期：** 2026-09-16（针对 0.4.2）
**本阶段性质：** 技术论证，不修改现有发布版程序

> **版本说明：** 本文初稿写于 0.4.1 时期，结论针对的是「方法」而不是某一版权重，因此整体不变。
> 2026-09-16 按 0.4.2 逐条核对了文中引用的模型事实与实测数字（第二节第二段、第三节、第七节、
> 第八节、第九节），并把 0.4.2 输出目录分两层、`batch_summary.csv` 列约定这两条新的集成约束写进
> 第七节。**将来实际开发时，以 0.4.2 为基线**；若那时已有更新版本，需再核一遍这一节的事实。

## 一、结论

Auto Worm ROI 在技术上可以接入具有视觉能力的大模型 API，但不建议让通用多模态大模型直接取代当前的高清晰度/低清晰度分割模型，直接负责生成最终线虫 ROI。

更合理的定位是：

1. 大模型负责识别图像类型和荧光表达模式；
2. 比较现有模型产生的多个候选分割结果；
3. 发现漏检、粘连、断裂和误识别；
4. 输出虫数、候选选择、疑点编号、提示点或外接框等结构化信息；
5. 由当前 U-Net、SAM/micro-SAM 等专用分割模型生成像素级掩膜；
6. 继续由 ImageJ 对原始 TIFF 和最终 ROI 进行荧光强度与 CTCF 计算。

综合判断如下：

| 项目 | 判断 |
| --- | --- |
| 接入 API 的工程可行性 | 高 |
| 大模型直接输出最终 ROI 的可靠性 | 低，必须实测 |
| 大模型用于候选选择和质量控制 | 中等，值得验证 |
| 大模型产生提示、专用模型完成分割 | 中高，最值得进行 PoC |
| 针对新表达模式扩充数据并重新训练 | 仍应作为长期主路线 |

因此，建议先开发一个与发布版隔离的实验性后端，完成小规模盲测后再决定是否正式集成。

## 二、当前问题的本质

现有高清晰度和低清晰度模型都是单通道灰度图像分割模型。推理时，图像经过各自的归一化方式处理后缩放到 **768 × 768**，再由 U-Net 输出背景、虫体内部和边界三路概率，随后通过规则生成实例掩膜。

这一组参数（`image_size`、`normalization_mode`、腐蚀次数、面积与高度阈值等）**逐 checkpoint 写在权重文件里，推理时从 checkpoint 读**（`batch_worm_roi.py` 的 `image_size = int(checkpoint.get("image_size", IMAGE_SIZE))` 一段），不是代码里的常量。0.4.2 随包的两个整虫权重实测如下：

| | 高清 `models/0.1.1/worm.pt` | 低清 `models/0.2.1/worm.pt` |
| --- | --- | --- |
| `image_size` | 768 | 768 |
| `normalization_mode` | `legacy` | `background_aware` |
| `training_dataset` | `base_n10_46_plus_supplement4_8` | `base_v020_76_plus_supplement3_20_plus_supplement4_8` |
| `trained_on_all_records` | `true`（54 张全进训练） | `false`（验证集是真留出的） |
| 留出/验证虫数正确率 | `holdout_count_accuracy = 4/4` | `supplement_validation_count_accuracy = 10/14` |
| 留出/验证 Dice | `holdout_mean_matched_dice = 0.9261` | `supplement_validation_mean_matched_dice = 0.8877` |
| `boundary_radius` | 6 | 6 |
| `postprocess_erosion` | 2 | 3 |
| `postprocess_min_area` | 0.012 | 0.003 |

读数注意两点：**高清那一列是 `holdout_` 前缀**，因为随包的这一版权重是 `--train-all` 出来的，它自带的 `val_*_at_best` 是在训练集上算的、不能当泛化能力读，可比的只有留出那一版；**低清那一列是 `supplement_validation_`**，验证集是真的留出的。两类数字不要混用（详见 `VALIDATION_0.4.2.md`）。

上表不是抄来的，就是上表下面这一段命令读出来的；将来换权重后照它重读一遍即可：

```bash
cd "<仓库>"
PYTHONUTF8=1 PYTHONIOENCODING=utf-8 \
  "<构建用 Python>" -c \
  "import torch,json; ck=torch.load('models/0.2.1/worm.pt',map_location='cpu',weights_only=False); \
   print(json.dumps({k:v for k,v in ck.items() if k!='model_state'},ensure_ascii=False,default=str))"
```

（`PYTHONUTF8=1` 与 `PYTHONIOENCODING=utf-8` **两个都要加**，否则打印出来的中文与路径会乱码。
`<构建用 Python>` 的路径见 `AGENT.md` 第 3 节。）

与 0.4.1 相比，0.4.2 换掉了两版权重（整虫模型由 `补充样例4` 的 8 张微调而来，再用新整虫模型重训了 tip 精修器）。**现有模型已经能在已覆盖的采集条件下取得较好结果**——高清在留出 4 张上虫数全对、平均 Dice 0.9261，低清在留出 14 张上 10/14、平均 Dice 0.8877。但训练分布仍然有限：两个模型的 `training_dataset` 都只有几十张量级（高清那一串里的 `46` 是基础集记录数，低清是 76 加两批补充），训练域之外仍然容易发生域偏移。**训练集的准确规模不要从本文抄**——以 checkpoint 元数据里的 `training_dataset` 与 `validation_records` 为准，它们是当场可读的（见下）。

### 0.4.2 自己就提供了一个域偏移的实例

这一点值得单独写，因为它把「大模型做 QC」从设想变成了有现成靶子的验证。

0.4.2 把 `补充样例4` 的 8 张（全是 0913 采集）加进训练集之后：**新域变好、旧域变差**。高清留出 4 张从 0/4 变成 4/4，但低清在 `boundary3`（旧域）的留出集上有 **2 张虫数反而错了**（`merge__g4__14` 10→9、`normal__g3__12` 10→11）。总账是净 +1（9/14 → 10/14），**但这不是全面提升**，而且本轮没有做任何补偿。

关键在于：**这两张的退化不是靠肉眼看 QC 图发现的，是靠留出集评测发现的。** 一个已经在跑的批次里如果混进了 `boundary3` 那类图，现有流程只会照常出一张 `qc_status=PASS` 的 QC 图和一行汇总——虫数错了也没人知道。这正是本文主张大模型在**第一阶段只做 QC**（不改最终 ROI）的现实理由：它要接的就是「留出集评测覆盖不到的那一半」——真实批次里没有金标准的那部分图。

不同表达模式造成的问题需要分开判断：

1. **整体亮度、对比度或背景发生变化**：可通过更稳健的归一化、数据增强和增加训练样本改善。当前高低清两版用的**不是同一套归一化**（`legacy` vs `background_aware`），低清那一版用的是更关注背景的一路——这条经验已经体现在代码里了。
2. **表达只集中在头部、中段或尾部**：模型可能把不发光区域误判为背景，需要包含此类模式的标注数据，或利用明场/DIC/体染通道辅助确定整虫轮廓。
3. **两条虫发生粘连或交叉**：需要实例分割、提示式分割或人工分界，单纯提高通用视觉理解能力不一定能解决像素归属问题。
4. **某段虫体在所有输入通道中完全不可见**：任何模型都无法从不存在的像素证据中恢复真实边界。大模型可以根据形态作推测，但这种推测不适合作为定量 ROI。

因此，这并不只是“当前模型规模不够大”，而是训练分布、可观测信息和分割任务形式共同造成的问题。

## 三、DeepSeek-V4.1-Flash 的适用范围

截至 2026-09-15，DeepSeek 官方 API 使用 `deepseek-flash` 调用 DeepSeek-V4.1-Flash。旧模型名会被路由到当前 Flash 模型。

该模型具备以下可利用能力：

- 支持图像与文本混合输入；
- 支持 PNG、JPEG、GIF 和 WebP；
- 支持 Base64、外部 URL 和 Files API；
- 支持 JSON Object 和 JSON Schema 结构化输出；
- 能进行物体识别、计数、粗定位、候选比较和视觉推理。

但其能力边界也很明确：

- 模型通过语言解码器自回归生成文本，没有公开的原生像素掩膜输出接口；
- TIFF 不能直接作为视觉输入，必须生成 PNG 等预览图；
- 较大图像会在服务端自动缩放，2048 × 2048 图像不能保持原始逐像素信息；
- 官方公布的 RefCOCO `Acc@0.5 = 86.0` 属于自然图像上的区域定位指标，并不代表显微图像逐像素分割能力；
- API 的滚动模型别名可能随服务升级改变实际模型版本，不利于科研结果完全复现；
- 输出坐标、多边形或 RLE 即使格式合法，也不代表空间边界准确。

所以，大模型“能够看懂图中大致有几条虫、哪一块像是粘连”，不等于它能稳定描出满足荧光定量要求的每个边界像素。

### 送进去的预览图必须与分割模型看到的是同一张图

这是本版新增的一条，也是实际开发时最容易埋雷的地方。0.4.2 的两个模型**各自有一套预处理**：不同的 `normalization_mode`，以及从 `image_size` 派生的缩放。如果给大模型看的预览图是另一种拉伸方式（例如直接线性归一化、或按原图长宽比缩放），那么大模型看到的东西**在数值上根本不是模型看到的**——它据此作出的“候选选择”和“这个候选漏了尾巴”的判断都失去了依据，而输出仍然是格式合法的 JSON，不会报错。

因此：**预览图应当走与 `process_image` 同一条预处理链路导出，或至少记录下两者的差异并且不要把后者当作判断依据。** 每张图要记录预览图的生成方式（哪一版归一化、缩放到的尺寸），写进第八节要求的可复现字段里。

## 四、为什么不建议直接让大模型输出最终 ROI

可以要求大模型返回 JSON 多边形、归一化坐标或 RLE 掩膜，但这种方案存在以下风险：

1. **边界精度不足**：细长虫体、尾尖和粘连缝隙对一两个像素的偏差很敏感。
2. **坐标幻觉**：结构化输出能保证 JSON 可解析，不能保证坐标真正贴合图像。
3. **输出量过大**：逐像素或高密度多边形会产生大量输出 token，并可能被截断。
4. **重复性不足**：相同图像重复调用可能得到略有不同的轮廓。
5. **定量误差被放大**：ROI 边界误差会同时改变面积、积分密度、背景均值和最终 CTCF。
6. **缺少领域验证**：现有公开结果不能证明该模型在荧光线虫实例分割上的效果。

第 1 条在本项目里尤其要紧，因为 **0.4.1 专门改过一次边界约定，0.4.2 沿用**：轮廓按**像素边界**导出（第 `c` 列的竖向边在 `x = c ± 0.5`），ImageJ 采样到的**恰好**是 Python 掩膜里的那些像素。这是花了一整轮、用 9 个合成掩膜和 44 个真实 ROI 逐个验过的不变量。0.4.2 **没有重验它**，但也没碰它（`CHANGELOG_0.4.2.md` 明确记着这条约定「未动」），所以它仍然成立——**「未动」不等于「重验过」，引用时要分清**。让大模型输出轮廓，等于在这条已经钉死的不变量上开一个「一两个像素的偏差无所谓」的口子——而它恰恰不是无所谓的。

因此，“DeepSeek 直接输出 ROI”应只作为实验对照组，不能未经验证进入正式定量流程。

## 五、推荐的混合架构

建议采用以下流程：

```text
原始 TIFF
    ↓
现有高清和低清模型分别生成候选掩膜
    ↓
生成无损 PNG 预览和带编号的候选轮廓图
    ↓
DeepSeek 返回结构化判断
    ├─ 预计虫数
    ├─ 推荐候选
    ├─ 疑似漏检/粘连/断裂的编号
    ├─ 提示点或外接框
    └─ 是否必须人工复核
    ↓
当前 U-Net 或 SAM/micro-SAM 生成、修正像素掩膜
    ↓
现有人工头向、人工分界、排除区域、部分圈画和形状修复
    ↓
输出 ROI ZIP
    ↓
ImageJ 使用原始 TIFF 计算荧光强度和 CTCF
```

这个设计使大模型发挥视觉理解和候选判断能力，同时把精确边界交给专用分割网络。原始荧光像素不会经过 API 转换后参与测量，因此不会改变现有 ImageJ 定量约定。

### 建议大模型输出的字段

```json
{
  "expected_worm_count": 10,
  "preferred_candidate": "low_clarity",
  "uncertain_instances": [2, 7],
  "suspected_merges": [[2, 3]],
  "suspected_missing_regions": [
    {"x": 0.18, "y": 0.64, "reason": "possible dim tail"}
  ],
  "prompt_boxes": [
    {"x1": 0.10, "y1": 0.42, "x2": 0.27, "y2": 0.83}
  ],
  "requires_manual_review": true,
  "confidence": 0.62
}
```

坐标建议统一为相对于图像宽高的 `0–1` 数值，返回后再映射到原始 TIFF。即便如此，提示坐标也只能用于驱动分割模型或提醒用户，不能直接作为最终 ROI。

**映射到原始 TIFF 时要用 checkpoint 里的 `image_size`，不要写死 768。** 两个模型现在都是 768，但 0.4.2 开发时就踩过一次「低清第一次启动漏了 `--image-size 768`、checkpoint 记成 512」的坑，导致工作分辨率被静默改变。`image_size` 是逐 checkpoint 的、并且**推理侧是从 checkpoint 读的**，所以按常量换算坐标迟早会错，而且是那种「大多数图看起来还行」的错。

## 六、几种方案的比较

| 方案 | 优点 | 主要风险 | 建议 |
| --- | --- | --- | --- |
| DeepSeek 直接输出多边形/RLE | 开发概念简单 | 边界不稳定、坐标幻觉、不可直接证明定量可靠 | 只做基准实验 |
| DeepSeek 在高清/低清候选中选择 | 不改变现有分割输出结构，容易接入 | 两个候选都错时无法补救 | 最适合第一阶段 PoC |
| DeepSeek 做虫数和错误类型 QC | 风险较低，可优先筛查失败图 | 不能自动修复最终边界 | 值得接入 |
| DeepSeek 产生点/框，SAM/micro-SAM 分割 | 理解与像素分割分工合理 | 增加模型和部署复杂度 | 最值得重点验证 |
| 直接微调当前 U-Net | 结果稳定、可离线、可复现 | 需要更多人工 ROI | 长期主路线 |
| 微调显微图像基础分割模型 | 泛化潜力较强 | 训练和打包更复杂 | 可与 U-Net 并行比较 |
| 增加明场/DIC/体染通道 | 为不表达区域提供真实边界证据 | 依赖采集条件 | 条件允许时优先 |

关于第 2 行「两个候选都错时无法补救」：0.4.2 的域偏移实例是一个具体的反例——低清在 `boundary3` 那 2 张上错的时候，高清未必对（两版模型训练域不同，错的地方也不一样），但**也可能两个都错**。所以这个方案的价值上限是「在两个候选里挑」，不会有别的；把它当作第一阶段的 PoC 是因为它**改动最小、失败也最安全**，不是因为它的收益上限高。

## 七、与当前程序的集成位置

当前 Java 插件负责启动 Python GUI、接收处理完成通知，并使用 ImageJ 读取原始图像和 ROI 做测量。API 不应放进 Java 测量层，而应放在 Python 推理层。

推荐增加独立的 `SegmentationBackend` 概念，例如：

- `local_high`：当前高清模型；
- `local_low`：当前低清模型；
- `local_dual`：两个模型都运行，再按规则选择；
- `llm_assisted`：两个本地候选加大模型判断；
- `prompted_foundation_model`：大模型/人工提示加专用分割模型。

现有 ImageJ 测量部分保持不变。无论使用哪一种分割后端，最终都应输出相同格式的 ROI ZIP，再由 ImageJ 对磁盘上的原始 TIFF 计算 `Area`、`Mean`、`IntDen` 和 `CTCF`。

### 落到 0.4.2 的代码上，具体要动哪里

上一条是设计层面的，这里把 0.4.2 的实际接口对上，免得开发时才发现要找的地方不止一处。

1. **后端开关的自然位置**是 `worm_roi_gui.py` 的 `MODEL_CONFIGS`（现在是 `high` / `low` 两档）与 `batch_worm_roi.py` 的 `run_gui_batch`。新增后端要同时出现在界面的模式选择与批处理的参数表里。
2. **`batch_summary.csv` 的列由 `RUN_COLUMNS` 统一定义**，表头、成功行（`_success_row`）、失败行（`_failure_row`）都按**列名**构造。第八节要求的「分割后端 / 请求模型名 / 响应 ID / 是否回退」这些字段要加进 `RUN_COLUMNS`，**由三处按列名同步生效**——不要再写位置拼接：少一个字段会让后面所有值整体错位，而 CSV 依然能被正常解析，只查行长度发现不了。这是本项目已经踩过的坑。
3. **输出目录分两层**：`<输出根>\measurements\`（只用 ImageJ 写的测量表）与 `<输出根>\other\`（ROI ZIP、QC 图、`batch_summary.csv`、五张 QC 明细表）。0.4.2 **只认这个新布局、不做旧目录兼容**。如果 LLM 的判断结果也要落盘（建议要，见第八节第 6 条），它属于「其余产物」，进 `other\`。
4. **新增任何按图命名的输出文件，必须同时加进 `IMAGE_OUTPUT_FILES`**（连同它该进哪个子目录）。这张表是文件归属的唯一真源，`_discard_partial_outputs()` 靠它把失败图的残留删干净。漏加的结果是失败图上残留一份文件，而不会报任何错。
5. **API 密钥不得走现有的界面配置持久化路径**。界面偏好写在 `.worm_roi_gui.json` 里，那个文件按第八节第 2 条属于「不得出现密钥」的地方。优先用系统凭据管理器或进程环境变量，并**显式确认它不会被顺手写进那张配置表**。
6. **云端调用要在 `run_gui_batch` 的异常分支里被当作可失败的一步**：失败必须退回本地结果并标记，而不是让整张图失败。注意 `run_gui_batch` 的现有约定是「单图失败不影响同批其余图像」，且失败图的结果会被 `_discard_partial_outputs()` 清掉——LLM 调用失败**不该**走这条路径，它是「降级」不是「失败」。

## 八、实验性功能的安全与可复现要求

如果以后正式实现，至少需要满足以下要求：

1. 云端功能默认关闭，并明确提示图像会被发送到第三方服务。
2. API 密钥不得写入 `.worm_roi_gui.json`、日志、CSV、崩溃报告或发布包。
3. 优先使用系统凭据管理器或进程环境变量保存密钥。
4. 网络失败、限流、超时或响应无效时，必须明确标记并回退到本地模型，不能静默混用。
5. 每张图记录：分割后端、请求模型名、返回模型信息、提示词版本、输入预处理方式、响应 ID、是否回退及失败原因。
6. 保存最终 ROI 和 QC 图，不能依赖以后重新调用滚动更新的云端模型来复现结果。
7. 上传未发表实验图像前，应核对实验室、单位和服务商的数据使用及保密要求。
8. API 转换图仅用于判断和分割，荧光测量始终使用原始 TIFF。

第 4 条在本项目里有一条现成的反面对照：桥接协议**新旧版本互不认识**（旧 jar 等的是 `AUTOWORM_IMAGEJ_BRIDGE_MANIFEST`，新 exe 只认 `AUTOWORM_IMAGEJ_BRIDGE_DIR`），配错版本会**静默什么都不做**。这个项目反复吃亏的地方从来不是「报错」，而是「出错了却不报错」。LLM 这条路径比桥接更危险——桥接配错了至少整批没有结果，LLM 静默混用则是**结果看起来完全正常、只是有一部分是云端猜的**。

第 5 条与第 6 条要合起来读：**滚动模型别名意味着「同一个请求今天和明天可能不是同一个模型」**，所以响应 ID 与模型信息不记下来，这份数据事后就无法复现，也无法解释某一批为什么和别的批次不一样。

## 九、建议的 PoC 验证方案

### 1. 建立测试集

首轮选择 40–60 张当前模型明显失败或不稳定的图像，至少覆盖：

- 全身均匀表达；
- 头部、中段或尾部局部表达；
- 一端极暗或中段断续；
- 高背景和脏背景；
- 虫体粘连、交叉和靠边；
- 不同实验日期、曝光条件、品系和荧光通道。

每张图制作人工 ImageJ ROI 金标准。测试集必须按实验批次或表达模式隔离，不能将同一实验中的相似图随机分到训练集和测试集，否则不能检验外推能力。

0.4.2 手上已经有一批现成的候选失败图，可以直接进这个测试集：

- `补充样例4` 的 8 张（0.4.2 的高清留出集，高清已全对，**低清在其中的表现仍可测**）；
- `boundary3` 留出 14 张里 0.4.2 数错的 2 张（`merge__g4__14`、`normal__g3__12`）；
- 既有现象三处：`0913-1k-2`（高低清两档都数出 11 条，预期 10）、`补充样例2` 里两张 `n=20` 的图（两档都数出 12 条）；
- `test01`…`test07` 与 `issues\0913` 的真实样例。

这四类的定位各不相同，用的时候不要混为一谈：

- 第一类是**已知可解**的（高清已经全对），拿它当正对照，用来确认流程没坏；
- 第二类是**已知退化、且本轮明确不做补偿**的（`AGENT.md` 第 9 节：没有按域分权重、没有加回放样本），是**最直接的目标样本**；
- 第三类是**已知的既有现象**，0.4.2 既没改好也没改坏，适合测「能不能稳定复现同一个判断」；
- 第四类**没有已知金标准**，要进这个测试集**必须先人工标注**，别当成现成的失败样本用。

也就是说：**只有第二、三类是 0.4.2 明确记为未处理的**，第一类已经解决，第四类只是素材。

### 2. 对照组

至少比较以下五组：

1. 当前高清模型；
2. 当前低清模型；
3. 两者中人工选择的较好结果；
4. DeepSeek 直接输出轮廓；
5. DeepSeek 选择候选或产生提示，再由 micro-SAM/SAM 类模型分割。

如果条件允许，再加入“用新增样本微调当前 U-Net”作为第六组。这样才能判断收益来自大模型本身，还是仅仅来自补充了新的标注数据。

**六组对照在本项目里有直接的先例意义**：0.4.2 已经证明「加 8 张新标注样本」本身就能把高清留出从 0/4 提到 4/4。也就是说，如果第六组（增量训练）没有被跑，那么第五组提升多少都不能归功于大模型——**它可能只是把等价的信息以更贵的方式喂了进来**。

### 3. 指标

需要同时评价分割准确性和定量影响：

- 每图虫数完全正确率；
- 匹配实例的 Dice；
- 边界指标，例如 NSD 或边界 F1；
- 漏检、误检、错误粘连和错误拆分次数；
- 相对人工 ROI 的面积误差；
- 相对人工 ROI 的 `IntDen` 和 `CTCF` 误差；
- 每张图处理时间；
- API 失败率和单图成本；
- 同一图像重复调用至少 3 次时的结果一致性。

CTCF 是最终业务指标，因此不能只凭“QC 图看起来更像”判断成功。即使 Dice 提升，若背景区或尾部边界改变导致 CTCF 偏差增加，也不能采用。

**面积误差这一项要对齐 0.4.1 的边界约定**（见第四节第 1 条）：以人工 ImageJ ROI 为准算面积误差时，比的是 ImageJ 侧的 `Area`，不是 Python 侧的掩膜像素数。0.4.2 的测量表把 `Area,Mean,Min,Max,IntDen,Median,RawIntDen` 放在前 7 列，就是为这类逐行核对准备的。

### 4. 决策方式

在实验前根据课题允许的定量误差预先规定通过标准。建议至少要求：

- 在完全独立的表达模式或实验批次上优于当前两个模型中的最佳结果；
- 虫数正确率、实例 Dice 和 CTCF 误差同时改善；
- 不增加严重粘连或漏检；
- 重复调用结果足够稳定；
- API 不可用时仍能安全返回本地结果，并明确标记后端。

如果大模型只能提高虫数判断和错误检出，而不能稳定改善像素边界，也仍可把它保留为“实验性 QC 助手”，不让它修改最终 ROI。

## 十、推荐实施顺序

### 第一阶段：不改最终 ROI

让 DeepSeek 查看原始图预览和高清/低清候选图，只输出：候选选择、预计虫数、问题编号和是否需要人工复核。验证它能否比现有数量 QC 更早发现外推失败。

**这一阶段的靶子已经在第二节写明了**：0.4.2 那 2 张 `boundary3` 退化图，是在留出集评测里发现的，不是靠 QC 图发现的。如果大模型能在一批没有金标准的真实图上把它们标成「疑似虫数不对」，第一阶段就算成立。

### 第二阶段：提示式分割

将大模型返回的点和框交给 micro-SAM、SAMJ 支持的模型或其他专用分割模型，输出候选掩膜；结果仍需经过当前 QC 和人工确认。

接入前要先想清楚**现有的人工分界与排除区怎么与提示式分割共存**：0.4.2 里人工分界线是**硬约束**（窄带内严格按线拆，见 `_snap_seam_to_guides`），排除区会在分割后把区域清零。提示式分割产出的掩膜若与这两者冲突，谁优先需要明确，不能靠调用顺序决定。

### 第三阶段：针对性再训练

把验证中确认的失败图及人工 ROI 加入训练集，比较：

- 当前 U-Net 增量训练；
- 显微基础分割模型微调；
- 大模型辅助方案。

最终选择独立测试集上最稳定、最可复现且 CTCF 误差最低的方案，而不是参数量最大的方案。

**这一阶段有现成的教训可循**：0.4.2 就是「加新域样本 → 新域变好、旧域变差」的一次完整实例，而且当时**没有做补偿**（没按域分权重、没加回放样本）。第三阶段若重训，必须同时看新域与旧域的留出集，只报新域数字是不足以支持决策的。

## 十一、最终建议

本项目值得进行多模态大模型 API 的小规模验证，但不应把通用视觉语言模型直接视为像素级分割模型。

推荐方向是：

> **DeepSeek 负责理解、比较和调度；U-Net 或显微图像基础分割模型负责像素边界；ImageJ 继续负责荧光定量与 CTCF。**

若实验图像包含整条虫的真实视觉证据，这种混合方案有可能提高跨表达模式的适应能力；若不表达区域在图像中完全不可见，则应优先增加明场/DIC/体染通道或人工提示，而不是依赖大模型猜测轮廓。

## 十二、参考资料

1. DeepSeek API Vision：<https://api-docs.deepseek.com/guides/vision/>
2. DeepSeek Responses API：<https://api-docs.deepseek.com/guides/responses_api/>
3. DeepSeek-V4.1-Flash 官方模型卡：<https://huggingface.co/deepseek-ai/DeepSeek-V4.1-Flash>
4. Segment Anything：<https://arxiv.org/abs/2304.02643>
5. Segment Anything for Microscopy：<https://github.com/computational-cell-analytics/micro-sam>
6. SAMJ-IJ：<https://github.com/segment-anything-models-java/SAMJ-IJ>
7. MedSAM：<https://www.nature.com/articles/s41467-024-44824-z>

## 十三、本文引用到的项目内文件

本机路径与仓库文档不再重复抄写，按 `AGENT.md` 第 2 节的路径地图查。

| 引用的事实 | 出处 |
| --- | --- |
| 两个整虫权重的 `image_size` / `normalization_mode` / 各阈值 / 留出指标 | `models/0.1.1/worm.pt`、`models/0.2.1/worm.pt` 的 checkpoint 元数据 |
| 逐 checkpoint 读参数、两个子目录名、`RUN_COLUMNS`、`IMAGE_OUTPUT_FILES` | `src/batch_worm_roi.py` |
| `MODEL_CONFIGS`、`.worm_roi_gui.json` 配置持久化 | `src/worm_roi_gui.py` |
| 人工分界线硬约束、排除区清零 | `src/manual_head_annotation.py`、`src/batch_worm_roi.py` |
| 0.4.2 的域偏移数字与三处未处理现象 | `CHANGELOG_0.4.2.md`、`VALIDATION_0.4.2.md`、`AGENT.md` 第 9 节 |
| 高低清留出集的来源与隔离方式 | `src/build_supplement_dataset.py`、`src/finalize_high_model_042.py`、`src/finalize_low_model_042.py` |
