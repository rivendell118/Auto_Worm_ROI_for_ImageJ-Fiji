# ---------------------------------------------------------------
# 自动圈虫 0.4.2 CUDA for ImageJ
# 只跟踪源码、测试、模型、文档与构建依赖；产物和大体积数据不入库。
# ---------------------------------------------------------------

# --- 打包与编译产物 ---
dist/
build/
# 发布 zip（约 3.1 GiB）由 make_release.py 重建，不入库
release/

# --- Python ---
__pycache__/
*.py[cod]

# --- 本机虚拟环境 ---
# .venv-build 在 0.3.1 副本里（构建用，含 torch+CUDA，约 6 GiB）；
# .venv-llm 在本仓库里（LLM 实验后端用，2026-09-16 由系统 Python 3.13.2 建）。
# 两者都是本机状态、体积大且可从 requirements 重建，不入库。
.venv-llm/

# --- 一次性探针的编译产物（审查用的 .java 会生成同名 .class）---
*.class

# --- 运行期残留 ---
# _atomic_write 会留下 <名>.<32位hex>.tmp.<扩展名>
*.tmp
*.tmp.*

# --- 可再生的验证输出（由 tests/ 里的集成测试重建）---
validation_output/
validation_outputs/
validation_gui_input/_auto_roi/

# --- 可再生的测试样例（合成 TIFF，由 diagnostics/make_test_samples.py 重建）---
diagnostics/test_samples/

# --- 大体积训练数据（144 MB，另有工作区备份）---
tests/train_data_v020/
tests/preview/

# --- 0.4.2 的训练集与训练产物（由 src/build_supplement_dataset.py 与 src/train_*.py 重建）---
training/

# --- 审查材料 ---
# review\ 只收**报告本身**（CHANGELOG 与 VALIDATION 引用的证据）。当年那个工作目录
# review_041_quant\ 在 2026-09-16 已整个删除、三份报告移进 review\，但审查往往会在
# 仓库旁边留下一堆一次性的东西——探针 .java/.py、夹具、日志、桥接残留、真实样例副本。
# 若再次在 review\ 里跑审查，这些一律不入库；**例外是 .md/.txt 报告**，否则新报告会被
# 这条规则连同垃圾一起吞掉。
review/*
!review/*.md
!review/*.txt

# --- 本机工具状态 ---
.vscode/
.idea/
*.swp
Thumbs.db
desktop.ini

# --- 真实实验图像：一张都不入库 ---
# 图像一旦进了历史，`git rm` 之后仍能从旧提交里按 blob 取出来——删文件和没发布过是两回事。
# 2026-09-16 为此改写了 0.4.2 与 LLM 两个副本的全部历史（见仓库外 devdocs\ 的落地记录）。
# 真实的图放仓库之外的 testdata\；合成样例在 diagnostics/test_samples\（上面已忽略）。
# 既有显微格式也有导出格式：交付出去、贴进汇报的往往就是 png/jpg，那同样是真实数据。
# 真要把某张图提交进仓库（比如文档配图），用 `git add -f` 显式放行——
# 这条拦的是「随手拖进来」，不是拦有意的决定。
*.tif
*.tiff
*.nd2
*.czi
*.lif
*.oib
*.oif
*.ims
*.png
*.jpg
*.jpeg
*.bmp

# --- 界面偏好（本机状态，不入库）---
# 现在写在 %LOCALAPPDATA%\AutoWorm\settings.json（见 worm_roi_gui.py 的 _state_directory），
# 仓库根或 src\ 下这一份是迁移前的遗留。2026-09-16 已从全部历史清除；
# 它一旦进了仓库，别人跑起来界面一开就默认指到开发机的文件夹。
.worm_roi_gui.json
# --- 开发文档与历史探针：不入库，集中在仓库外 ---
# AGENT.md 是交接文档，通篇是本机的目录布局（77 处绝对路径）；tests\ 里那批历史探针
# 有 29 个带着本机路径。它们同时是某些实验现象的唯一记录，所以是**搬走**而不是删掉。
# 2026-09-16 从全部历史里清除，现在放在仓库外：
#   AGENT.md -> 仓库外的 devdocs\（AGENT-0915-0.4.2.md / AGENT-0916-LLM.md）
#   探针     -> 仓库外的 probes\（两个副本里这批文件逐字节相同，只留一份）
# 下面把名字列全，是防止被顺手拷回来——**要放回来，先想清楚里面的路径怎么办**。
AGENT.md
tests/_diff.py
tests/_diff_broi.py
tests/baseline_768.py
tests/check_isize.py
tests/compare_models.py
tests/contact_train_log.txt
tests/debug_contact.py
tests/debug_split2.py
tests/dump_meta.py
tests/dump_meta2.py
tests/erosion_0101_probe.py
tests/eval_contact_model.py
tests/eval_split.py
tests/eval_watershed.py
tests/geom_merged.py
tests/gt_analysis.py
tests/param_scan.py
tests/probe_0101.py
tests/probe_merging.py
tests/probe_merging2.py
tests/proto_split.py
tests/proto_split2.py
tests/recheck_n20.py
tests/survey_merging.py
tests/sweep_production.py
tests/verify_contact.py
tests/verify_contact_br6.py
tests/vis_geometry.py
tests/vis_merging.py
