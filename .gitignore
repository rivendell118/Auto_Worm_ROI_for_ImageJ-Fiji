# ---------------------------------------------------------------
# 自动圈虫 0.4.2 CUDA for ImageJ
# 只跟踪源码、测试、模型、文档与构建依赖；产物和大体积数据不入库。
# ---------------------------------------------------------------

# --- 打包与编译产物 ---
dist/
build/
# 发布 zip（约 3.1 GiB）由 make_release.py 重建，不入库
release/

# --- Python ---
__pycache__/
*.py[cod]

# --- 本机虚拟环境 ---
# .venv-build 在 0.3.1 副本里（构建用，含 torch+CUDA，约 6 GiB）；
# .venv-llm 在本仓库里（LLM 实验后端用，2026-09-16 由系统 Python 3.13.2 建）。
# 两者都是本机状态、体积大且可从 requirements 重建，不入库。
.venv-llm/

# --- 一次性探针的编译产物（审查用的 .java 会生成同名 .class）---
*.class

# --- 运行期残留 ---
# _atomic_write 会留下 <名>.<32位hex>.tmp.<扩展名>
*.tmp
*.tmp.*

# --- 可再生的验证输出（由 tests/ 里的集成测试重建）---
validation_output/
validation_outputs/
validation_gui_input/_auto_roi/

# --- 可再生的测试样例（合成 TIFF，由 diagnostics/make_test_samples.py 重建）---
diagnostics/test_samples/

# --- 大体积训练数据（144 MB，另有工作区备份）---
tests/train_data_v020/
tests/preview/

# --- 0.4.2 的训练集与训练产物（由 src/build_supplement_dataset.py 与 src/train_*.py 重建）---
training/

# --- 审查材料 ---
# review\ 只收**报告本身**（CHANGELOG 与 VALIDATION 引用的证据）。当年那个工作目录
# review_041_quant\ 在 2026-09-16 已整个删除、三份报告移进 review\，但审查往往会在
# 仓库旁边留下一堆一次性的东西——探针 .java/.py、夹具、日志、桥接残留、真实样例副本。
# 若再次在 review\ 里跑审查，这些一律不入库；**例外是 .md/.txt 报告**，否则新报告会被
# 这条规则连同垃圾一起吞掉。
review/*
!review/*.md
!review/*.txt

# --- 本机工具状态 ---
.vscode/
.idea/
*.swp
Thumbs.db
desktop.ini
